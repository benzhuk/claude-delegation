// Compatibility entry point. The single implementation ships with the decisions skill.
export { DEFAULTS, findProjectRoot, loadProjectConfig, switchedOff } from "../skills/decisions/scripts/project-config.mjs";
