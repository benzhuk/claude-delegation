VERDICT: ROOT_CAUSE_CONFIRMED_AND_USEFUL_NATIVE_REVIEW_DELIVERED; GUIDE_CORRECTION_REQUIRED

September24,2026, America/New_York. The original first-use pilot remains failed. Its claimed external policy dependency was incorrect: the fixture used `--ignore-user-config` and thereby suppressed selection of the existing elevated Windows sandbox. Codex0.156.1 headless execution defaults to Never approval; pinned source tests forbid unmatched read-only commands when the Windows backend is Disabled. Existing provisioning does not itself select the backend. [Source diagnosis](codex-readonly-source-diagnosis.md) identifies official tag/commit and exact code/tests; root independently spot-checked the disabled-backend decision, backend resolution, and headless Never override.

The corrected invocation explicitly selected `windows.sandbox="elevated"`, keeping `--ignore-user-config`, `--sandbox read-only`, approval Never, existing execpolicy rules and invocation-only disabled hooks. This restored the already requested enforcement backend; no rule, permission profile, global shell, persistent config, auth or repository content was changed.

Native Codex Terra medium reviewed the existing everyday-work guide against actual goal/work-record files and installed mirrored continue/team-build. Source was `158dfb265964f98e7683a06d3e7e4c73e6d86e68`, mirror0.20.3. Four read commands completed successfully through Store PowerShell under the elevated read-only sandbox; zero policy denials, empty stderr, actual exit0, no timeout. Run lasted44.322seconds, 6:42:09–6:42:53AM America/New_York. Provider-reported usage:152934input,116480cached input,2758output,1506reasoning output; do not sum subset counts. Cost not reported; this is not whole-project cost.

Reviewer returned NEEDS_FIXES: the guide's closure conditions omitted explicit user stop, contrary to continue. Original report preserved at [native-codex-readonly-review-original.md](native-codex-readonly-review-original.md), SHA256 `c732e893a1aec9f751cfb4863f73b8fe5451d433f3533dbc2a0a89cd0b286c54`. Root corrected that sentence at6507f00. Useful output can contain a valid rejection; exit0 does not mean the reviewed guide was accepted.

Private local trace: `C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/native-codex-readonly-review-run/` contains launch.json, summary.json, stdout.jsonl, stderr.log and the original review. Watched config, hook-registration, guide and mirrored-skill hashes remained unchanged. Authentication was used in place, never copied. This establishes source-grounded useful native model review under a corrected read-only fixture. It does not establish hook lifecycle, production launch compatibility, unattended diligence, mixed-peer delivery, comparative speed or later rework.

The separate Store/MSIX outer-hook-shell defect remains distinct: this pilot disabled hooks and normal tool execution has its own shell compatibility path. The reviewed upstream report was posted, with Ben's explicit authorization, as https://github.com/openai/codex/issues/47810 after searches and inspection of related35863/33564 showed no equivalent boundary-specific report. Root read back title, author benzhuk, OPEN state and URL. Do not wait on the upstream fix; qualify the supported current host route and continue useful work.
