VERDICT: PASS — executable candidate 3f4d213c61edaa0deab996eea3522653d8b37307

September 23, 2026 (America/New_York). The sealed runner passed 1,322 tests, zero failures/skips, in 40.659 seconds. HEAD matched before and after the gate. Both changed skills passed the existing validator in UTF-8 mode. Independent Astra approved the exact candidate in delivery-corrections-review.md; strict pinned-artifact acceptance passed. The pinned candidate is deliberate; later commits add acceptance evidence only.

The real Windows argv regression exercises the production queue function through an actual Node child process with an inert npm-layout fixture. Message metacharacters remain a single argument. Separately, the real resolved launcher ran only --version and returned codex-cli 0.156.1. No real queue call or model invocation was part of verification.

The initial child guard was rejected because its 16 KiB cap could not read actual 22,834-byte child metadata. The corrected guard reads bounded chunks up to 256 KiB, and the pure classifier returned child=true / parent=false for actual local metadata. Representative-size and adverse-input tests preserve unknown metadata behavior and verify no child registration/inbox access. This proves the classifier and adapter source seam; the running host's child-event payload remains a separate observation.

The missing Fable notes remain in the ledger. Their seen cursor caused retries to retire them; original delivery was not found in the inspected lead transcript. Hook trust hashes matched, and a Windows launcher failure was independently reproduced. Child consumption is a source-backed risk, not an attributed historical event. No transcript, cursor or historical note was rewritten to manufacture delivery evidence.

No new timer, scheduler, model loop, hook registration or ongoing-work authority binding was introduced. The runtime continuation requirement is still open. The independent private-capture repair proceeds in its own checkout so it does not delay these reviewed corrections.
