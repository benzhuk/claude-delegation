VERDICT: PARTIAL

Actual Codex0.156.1 with GPT-5.6-Terra used existing ChatGPT authentication to deliver a proposed first-project guide on September23,2026,11:46PM America/New_York. Authentication required no new login, config change or credential copy. This qualifies the earlier statement that a disposable native PACKAGE route needs attended authentication: the already-authenticated plain-mirror route can run a model.

The source-grounding requirement failed. Three shell commands, including reads of the requested files and Get-Location, returned `rejected: blocked by policy`. The model disclosed that it had not read the skills or repository documents. The error does not identify a filesystem permission cause; the draft's attribution to a filesystem policy is not established. No policy bypass, ignore-rules flag, new trust or persistent change was attempted. The task ended after27.180seconds with exit0 and a clearly limited draft; exit0 is not successful skill use.

Invocation used `exec --ephemeral --ignore-user-config --disable hooks --sandbox read-only -m gpt-5.6-terra`, medium effort, existing auth home, retained execpolicy rules, and a temp output-last-message artifact. Both current/default personal config and hooks hashes stayed unchanged. Hooks were disabled only for this invocation, so no lifecycle or continuation proof is claimed.

Exact authored draft: [native-codex-first-use-original.md](native-codex-first-use-original.md), SHA256 `2a393d013e54bf85934b847f69f749b3cd1859adcbfee90445f5b3bff92996c7`. Native thread `01a0d185-5e33-7ec1-b9f6-6499ada85d9a`; final usage input86169, cached73472, output1357, reasoning393. Monetary cost unavailable. Raw launch, stdout, stderr, brief and summary: `C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/native-codex-first-use-run/`.

Parent has ordinary source access and can review/correct the delivered prose against the actual files. Doing so produces attributable mixed author/reviewer documentation; it does not retroactively establish the native author's file access, use of the installed skills, or the product speed/quality goals. No further trial with weakened policy is selected.
