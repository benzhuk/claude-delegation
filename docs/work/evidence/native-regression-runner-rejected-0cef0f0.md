VERDICT: NEEDS_FIXES 0cef0f073aea3364fc5c4f1c23f45d43c054c0cd

Independent scope: scripts/native-continuation-smoke.mjs and docs/native-continuation-testing.md only. Targeted diff against named artifact was empty. No repository source edits; reviewer did not build or plan this runner.

Cause: portable source, native protocol and model-visible argument sourcing are substantially repaired, but success predicates still equate quiet Stop with completed accounting, and timeout ownership stops at the direct child rather than its process tree.

Discriminating check: actual runner invoked by absolute source path from C:/Users/benzh/AppData/Local/Temp with exact C:/Users/benzh/.local/bin/claude.exe. Native 2.1.281 run passed all three scenarios. Output: C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/native-runner-independent-0cef/summary.json. Unaccounted: 4 requests, 96,637-byte transcript, 1 block, 1 refire. Accounted: 4 requests, zero blocks, final phase active. Interrupt: 5 requests, native control acknowledgement, STALE_EPOCH and final unbound. Independent adverse harness native-runner-independent.test.mjs ran through scripts/run-tests.mjs: 2 tests, both fail. It extracts the exact runner function/predicates from source without changing repository files. Only owned fixture processes were used; the surviving test grandchild was explicitly cleaned by its captured PID. Relative executable, missing executable and nonempty output CLI invocations were rejected.

1. HIGH: accounted success does not require accounting or an active bound snapshot.
Path: scripts/native-continuation-smoke.mjs:120-127, especially 126; docs/native-continuation-testing.md success claim.
Repro: exact extracted success predicates accept name=accounted, native exit 0, one PostToolUse, one Stop, blocks=0, finalPhase=unbound, requests=1, and no visible bind result. No account result or accountedRevision is checked. A regression that loses bind activation or disables continuation for the accounted case can pass as quiet accounting. The actual positive native run does not disprove this false-positive gate.
Fix location: track successful account JSON from actual model-visible tool results; assert current matching session state is active and its accountedRevision equals the observed bind/account revision; require expected binding/accounting progression and no attempted correction. Use private state solely for these post-execution assertions, preserving current provider argument sourcing. Add an adverse test with missing/failed account and inactive binding that must fail nonzero.

2. HIGH: timeout does not bound owned descendant lifetimes; server cleanup has no independent bound.
Path: scripts/native-continuation-smoke.mjs:37-46, generated wrapper at 84, and cleanup at 119.
Repro: exact runChild function starts an owned Node child that starts a long-lived Node grandchild. At a 500ms test timeout, runChild rejects but process.kill(grandchildPid,0) succeeds: descendant remains alive. On actual execution the analogous descendants are Bash/hook-wrapper/hook processes. The wrapper itself has no deadline. Additionally server.close(callback) waits for active connections without a deadline or forced close; an orphan retaining a request can prevent the advertised timeout from finishing cleanup. Current docs promise bounded processes and servers beyond what this implementation provides.
Fix location: maintain ownership of the native process tree and terminate/await only that owned tree on timeout/error, including wrapper children; settle the timeout path once. Bound server shutdown and close owned active sockets on failure. Preserve logs/output, and never use killing as interruption evidence. Add hanging-host-with-descendant and open-connection cleanup regression probes.

Positive source findings: exact supplied executable is invoked directly; plugin root derives from import.meta.url; output rejects nonempty destinations and does not recursively delete them; child scenario env is allowlisted with disposable home/config/state, dummy key and loopback endpoint; source contains no ephemeral driver dependency; synthetic provider derives host/session/epoch/revision from native Messages-visible context/tool results; private states are read only after child completion. Default-prompt >64KiB regression is exercised. Native interruption uses control_request rather than a kill signal.

Simplification: retain the single portable runner, one shared server/process driver and current three cases. Strengthen a few outcome assertions and make owned cleanup actually bounded; no production runtime, scheduler, provider abstraction or extra settings format is needed. Native runner approval would still not establish overall product, Codex or useful-model behavior.
