VERDICT: NEEDS_FIXES e3d7ff7151b65a8bdfedf67119700270ff82e27f

Independent Codex source/package review, September 23, 2026, America/New_York. Scope: continuation-native normalizer, Codex adapter/tests, native hook manifest, compatibility plugin manifest/root removal, package tests and Codex README. No source changes by reviewer. Actual-native full-core/package qualification is a separate pending gate.

Finding (four fields)
1. Severity/location: MAJOR isolation regression, hooks/continuation-native.mjs classifyCodexRole, consumed by hooks/multi-codex-hook.mjs isConfirmedCodexChild before inherited slug handling.
2. Trigger/evidence: previously positive legacy child identity is callback.session_id=child, transcript session_meta.payload.id=child, source.subagent.thread_spawn={parent_thread_id:parent,depth:1}, no callback.agent_id and no transcript session_id. Prior source classifies it child. Candidate requires payload.session_id equal callback.session_id before inspecting spawn and therefore downgrades it to unknown. Independent executable reproducer: C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/codex-legacy-child-review.mjs; saved summary C:/Users/benzh/AppData/Local/Temp/codex-legacy-child-review-7GdmJg/summary.json. Actual runCodexHook with inherited NOTE_SLUG parent called injected parent inbox once, returned decision:block and ackIds [parent-ask], and wrote parent registry entry threadId=child. The helper returned an ACK slice; the fixture did not call its ACK callback or contact a real peer.
3. Impact: a child whose prior positive structured identity was suppressed can now register over its parent's inbox and consume/ACK parent notes through the ordinary emit path. Continuation itself stays unknown/nonblocking, but peer isolation regresses. Existing tests were changed to the new payload shape rather than retaining the earlier contract regression case; all 16 focused tests pass despite this bug.
4. Required fix/check: preserve two explicitly validated positive-child contracts. Keep new native 0.156 parent-session + agent-id + transcript-id/session-id matching. Additionally retain legacy own-session callback with exact transcript id, absent agent_id, valid distinct structured parent UUID and integer positive depth, without broadening lead classification. Conflicting supplied session metadata must stay unknown. Add both positive forms and conflicting/malformed negatives, depth>1, and a real adapter test proving zero registration, inbox reads or ACK slice for the legacy child with inherited slug. Do not infer child or lead from slug.

Source/package findings otherwise
- New exact lead identity accepts cli and vscode only, requires transcript id=session_id and matching payload.session_id, no agent_id. The explicit new child branch binds agent_id to child transcript id, retains positive depth and distinct parent. Inherited slug does not establish continuation role. Unknown remains nonblocking in the core.
- Production normalizer profile enables codex-native-turn-v1 without a user capability flag; this is justified only after the separate actual native gate proves the production command adapter, current native turn identity, Stop/refire and Interrupt. Dependency injection can explicitly disable it. Native-core qualification remains pending here.
- SessionStart/UserPromptSubmit/PostToolUse/Stop/Interrupt use one installed-root command into the existing shared adapter. Interrupt does not synthesize a continuation. Core state remains keyed by host/native session; exact turn event identity and explicit bind/account authority checks are unchanged.
- Removing root portable manifest and adding only .codex-plugin/plugin.json is an intentional loader-compatibility choice, consistent with supplied actual 0.156 loader evidence. Thin manifest exposes the same shared skills and a Codex-specific hook configuration; Claude manifest/hooks are untouched. No alternate runtime or per-hook PowerShell workaround was added. Installed ${PLUGIN_ROOT} expansion and full real-core behavior require the separate native gate, not these JSON assertions.
- Marketplace continues resolving repository root. Nine expected shared skill directories are pinned. README warns against automatic duplicate mirror/native-hook installation, describes trust, and records legacy-loader baseline. No new source defect found in these package paths.

Independent focused gate
node scripts/run-tests.mjs hooks/continuation-native.test.mjs hooks/multi-codex-hook.test.mjs scripts/native-package.test.mjs -> 16 passed, zero failed/skipped; sealed-home-tYJmzi. Added independent temporary adversarial reproducer above exposes missing compatibility coverage. No full suite, real configuration edits, provider calls, or peer messaging performed.

Release boundary
Do not interpret source review as native acceptance. After fixing the child regression, require independent installed-package actual real-core bind->PostToolUse->Stop block/refire, account silence, Interrupt disarm, no-slug behavior, and genuine native parent/depth-child isolation. Preserve the negative summary as a regression artifact.
