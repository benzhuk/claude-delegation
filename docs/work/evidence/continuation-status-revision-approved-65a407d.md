VERDICT: APPROVE 65a407dfbbb28313f2b24d95b8cd93e988149954

Independent Astra review of scripts/continuation.mjs and scripts/continuation.test.mjs against docs/specs/2026-09-23-continuation-status-revision.md at f1f1a02. No source edits. Working copies of both reviewed files matched the exact candidate (git diff candidate -- these files returned empty).

The repair adds selectionStatus and revision only to active status, using the same bounded validated selection reader already used by bind/account/Stop. Valid current evidence yields its current digest. Invalid/missing/unreadable evidence yields unknown and null, while accountedRevision remains explicitly historical. Pending/unbound/stopped behavior stays unchanged. Status takes no claim, writes no state, grants no authority, and cannot refresh an old epoch. Account still independently checks epoch and re-reads the current selection. This is the small architectural repair called for, with no duplicate digest/cache/ledger.

Observed checks:
- node --test scripts/continuation.test.mjs: actual process exit 0, 21 passed, 0 failed. Retained output: continuation-status-revision-focused-review.log.
- Independent public CLI/API scenario script: actual process exit 0, 13 invocations. Active evidence change produced a different valid revision, old revision account exited 3 with STALE_REVISION, current revision accounted successfully. Malformed, missing and oversized evidence each produced active + unknown + null, never the previous accounted revision as current. State, record, authority, evidence bytes and state-directory entries stayed identical across status calls. A filesystem proxy rejecting all mutation methods still allowed valid status. Replacement prompt then rejected the prior observed epoch/revision with STALE_EPOCH. Retained source and output: continuation-status-revision-independent.mjs and continuation-status-revision-independent.log.
- Focused suite additionally covers EACCES selected evidence, pending/stopped status JSON, and existing native-facing lifecycle/selection boundaries.

No blocking findings in this delta. Status is an observation of the selection during its bounded read, not an atomic repository snapshot or new authorization; existing account checks handle the later command boundary. Malformed state still follows the pre-existing safe lifecycle behavior rather than gaining a revision. These checks use real Node CLI processes and fixture hook events, not a real Codex/Claude model session. Root owns the full sealed integration gate and useful native qualification.
