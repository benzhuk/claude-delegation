VERDICT: READ_ONLY_SANDBOX_STARTUP_FAILURE_REPRODUCED; APPARMOR_CAUSE_STRONGLY_SUPPORTED_NOT_AUDIT_PROVEN

September24,2026,America/New_York. This is separate from Windows SDK issue47810. No provider calls, policy changes, installations, credentials or historical-session resumes were used. The task-owned remote terminals were closed after each observation. Original private reports are0924-hetzner-next-diagnosis.md,0924-hetzner-sandbox-discriminator.md and0924-hetzner-capability-boundary.md under Temp/astra-followthrough-0923.

The earlier public Codex0.154 JSON omitted actual tool attempts. Its owned trial trace contained three exec calls denied with permission_denied. User-rule and hook inspection did not explain the host difference: Hetzner has no applicable user rules; Netcup's own execpolicy check matched zero rules for the successful read; both shared hook adapters have identical hashes and no permission-decision output.

Installed CLI help supplied the actual local syntax: codex sandbox -P :read-only --include-managed-config -C PRIVATE_TEMP -- COMMAND. Both /bin/true and the exact installed continue-skill read fail on Hetzner before execution, exit1, empty stdout, with bwrap loopback RTM_NEWADDR Operation not permitted. Identical explicit probes on Netcup exit0; its read returns6120bytes with the expected SHA256fdaf7f318143c3420d7c091b18cb487eec835dec7dea8a27d0a89b3c9c269f99. Host-side file readability is not the failing boundary. This direct helper failure strongly corroborates, but does not directly trace, the historical native admission path.

Hetzner runs Ubuntu kernel6.8.0-136-generic with apparmor_restrict_unprivileged_userns=1, no PATH bwrap and no narrow bwrap-userns-restrict profile. Netcup's Debian kernel6.12.100+deb13-amd64 has no readable restriction sysctl. Both contexts otherwise have zero effective/permitted/ambient capabilities, identical bounding capabilities, NoNewPrivs0, Seccomp0, identity UID/GID maps, unprivileged_userns_clone1, an unconfined current label and no detected container. Those common indicators do not explain the difference.

The exact error is already reported in [Codex issue14919](https://github.com/openai/codex/issues/14919). [Official sandbox prerequisites](https://developers.openai.com/codex/concepts/sandboxing#prerequisites) describe preferring distro Bubblewrap and the narrow Ubuntu AppArmor profile. These match the failing host's missing prerequisites; they do not by themselves prove the kernel's EPERM issuer or that a Codex version upgrade would fix it.

The final targeted kernel-journal query had restricted visibility and no matching visible record: absence is not exonerating evidence. Loaded AppArmor policy at /sys/kernel/security/apparmor/profiles was denied, aa-status exited4, AppArmor was enabled, and the named profile paths were absent. Administrator-visible audit and policy inspection are the specific remaining diagnostic dependency. No privileged retry or global restriction disable was performed.

Keep useful Codex work on the already-qualified Windows and Netcup routes and Claude work on its qualified hosts. A scoped OS prerequisite repair is a separate owner choice, not an upstream release dependency or a reason to patch the agent's prompt. If selected, inspect the exact audit/executable path and package plan, install only the relevant distro prerequisites and matching narrow profile, retain the global restriction, then repeat the no-op/read probes before any bounded native model qualification. Source changes to the harness are not indicated by this evidence.
