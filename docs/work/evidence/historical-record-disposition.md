VERDICT: PASS historical archival, not fabricated acceptance

A bounded source/history scout confirmed eight stale active records: package P1–P4 and rename R1–R3 still runnable, P5 still delivered. Earlier census/bearings evidence already identified the mismatch, but no disposition had been applied. The current workspace therefore contradicted the recovery summary's suggestion that reconciliation was complete.

Actual `git merge-base --is-ancestor` checks each exited0 for the four package artifacts and integrator `a185dc82` against released `cad92e9` (0.12.0), and for rename artifacts `57b4372`, `3b64350`, `a583e97` plus seam `5012a4a` against `5392b9e` (0.13.0). The scout's initial rename-release association was corrected before action. Existing package run evidence is preserved in `package-build-run-return.md`.

Astra moved exactly those eight files to `docs/work/archive/2026-09-22/`, keeping their bytes and missing historical fields unchanged. All resolved source and destination paths were checked inside the integration workspace, existing targets and reparse points were refused, and each before/after SHA256 matched. Hash receipt: `C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/historical-record-archive-hashes.json`. No other record or source implementation moved.

Direct invocation of the existing nonrecursive `listRecords('docs/work')` after archival found zero retired records. No current Children reference or filename caller targets those records; remaining citations are historical evidence or P5's historical work ID. This corrects current queue admission without adding a tracker, parser flag or invented acceptance date. The original status evidence remains readable in the archive and Git history.
