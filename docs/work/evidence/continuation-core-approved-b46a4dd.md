VERDICT: APPROVE b46a4ddb8469fd3e61442d8c41f71ffd3923c329

Scope: exact scripts/continuation.mjs, scripts/continuation.test.mjs and scripts/continuation-contract.mjs at the named artifact only. Current integration HEAD had advanced to 2b21a3711584528c55d6737ebd6d830cee1010a7, but git diff b46a4ddb8469fd3e61442d8c41f71ffd3923c329 -- those three paths was empty. Adapter files, host capability qualification, publication and live installation are not approved by this report. Work: wr-2026-09-23-continuation-core.

Cause: the prior five findings came from a CLI contract mismatch, lifecycle invalidation after identity rejection, unconfined validator I/O, ignored unreadable index entries, and repeated dependency normalization. The current changes address each cause in the core rather than granting adapter or CLI authority.

Discriminating check: independently reran node scripts/run-tests.mjs scripts/continuation.test.mjs C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/continuation-independent.test.mjs : 27/27 passed. Independently added and ran node scripts/run-tests.mjs C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923/continuation-round2-independent.test.mjs : 2/2 passed. The additional harness launches six actual child processes concurrently against one armed session and verifies exactly one Stop reason; separately corrupt JSON, null/incomplete current state and deleted state produce no Stop block. Tests stayed within the sealed runner and fixture home; no provider/network/real-home/full-suite operations or source edits.

Finding closure:
1. CLI marker: bind success now includes continuationBind.requestId and continuationBind.epoch. Durable actual child-process CLI assertion and prior independent structured-marker test pass.
2. Stale authority: authenticated lead SessionStart suspends before identity checks; invalid/unknown new lead prompt identity, missing event key and unsupported profile suspend before returning. Previous unknown-prompt/stale-Stop reproduction no longer blocks, and source subtests cover the other early-return variants.
3. I/O bounds: authority/record/evidence content is loaded through descriptor open/fstat/read with a per-file cap and aggregate accounting; evidence is preloaded before validateRecord, whose evidence operations use the confined cache. Durable descriptor-read observation verifies oversized evidence has zero content reads, avoiding the previous final-UNKNOWN-but-already-read flaw. Files growing past the allocated cap are rejected using a one-byte probe. This establishes finite content reads, not a wall-clock filesystem-latency guarantee.
4. Unreadable duplicate: record-load errors and non-file index entries now return INDEX_UNCERTAIN rather than disappearing. Durable openSync-denial test exercises the actual descriptor path. The older readFileSync-injected duplicate test also passes, but alone would not prove this repair after the reader API changed.
5. Dependency propagation: depsOf preserves normalized fs/epoch/nonce dependencies. Durable injected openSync denial through bind/account/Stop passes, proving the real callers no longer fall back to ambient fs. The original injected authority denial case also passes.

Fix location: scripts/continuation.mjs bounded loader/cache and selector, early prompt/start invalidation, depsOf and bind success payload; durable regressions in scripts/continuation.test.mjs. No further blocking core finding in this delta review.

Simplification: keep the one-state-file/one-claim design and existing work-record accounting. Optional cleanup: honorInjectedReadPolicy is a compatibility probe solely for wrappers intercepting the former readFileSync API; after fixture migration to open/read injection, remove that invalid-flag probe instead of expanding it into a second read-policy mechanism. This is not required for the observed production core behavior.

Limits: independent cross-process test demonstrates a single reservation under six competing processes; it does not prove bounded wall-clock behavior on a hung filesystem. Directory enumeration and realpath/open races have not been exhaustively fault-injected. Core approval does not establish that native callers authenticate profile/role/episode/bind markers correctly. Parent still owns exact integrated seam and actual adapter-native gate, including the renamed claude-code-stop-v1 capability profile.
