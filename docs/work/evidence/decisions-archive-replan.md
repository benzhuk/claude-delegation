VERDICT: RE-PLAN — retain the strict shared-reader boundary; normalize positively identified historical page structure rather than classifying every non-active heading as history.

Evidence: sourcea05cf86861b6756e3a0e2041c2e5e334c27c938f, current reader, hand-back shapeLines, decision template and skill. Parent supplied the private-page structural discriminator: five optionless summaries are within Closed, three are under a later separate H1, followed by another H1. I did not reread or reproduce private page content. The original “all eight within Closed” premise was incorrect. Reducing8→3 is the expected result of the approved boundary, not evidence of another parser bug.

Recommendation: keep the rule that only explicitly declared Closed scope exempts optionless historical summaries. Under the parent's existing authorized page-edit route, demote only positively reviewed historical sibling sections into the Closed hierarchy (H2 or canonical nested headings as appropriate), preserving every heading title, paragraph, summary, checkbox, comment, order and Done state. This makes the page declare its actual semantics. It is a bounded content-structure migration, not a parser title exception and not deletion of history. Do not sweep arbitrary H1 sections into Closed merely because their summaries lack options. The parent must positively identify the historical sections; unknown sections stay outside and remain subject to shapeless validation.

Why not invert to “only Waiting on you now is active”? That rule would let an accidentally renamed active heading, a new unrecognized active section, or a malformed decision beneath an unrelated H1 evade validation. Supporting a fallback for missing canonical active anchors still leaves mixed canonical/unknown pages unsafe. Explicit history is the smaller and safer contract. Avoid hardcoded historical title lists or broad narrative heuristics.

There is existing architectural duplication: decisions-handback.mjs shapeLines scans backward for raw column-zero H1 text and filters shapeless to Waiting/no-heading. Its comment explicitly records an earlier choice to retain historical extra sections. This explains why attended hand-back and pickup disagreed. Once the reviewed page hierarchy conforms, simplify hand-back to report the shared doc.shapeless directly, retaining its existing outward SHAPE format. Do not move that permissive filter into the shared parser. This is a deliberate change to the earlier hand-back exception and should be recorded in the amended spec/work record; parent should reconcile its prior “keep as-is” rationale with current authorized hierarchy-only migration.

Contract revision:
- Correct the live premise and distinguish source repair from page migration. a05cf86 should continue yielding three shapeless entries on the unmodified private page.
- Synthetic mixed page: explicit Closed histories exempt; unknown subsequent H1 plus optionless summary remains shapeless and actual pickup INVALID, including Done=false.
- Synthetic normalized counterpart: same exact narrative/human text and ordering, only reviewed historical heading hierarchy differs; zero shapeless, while current malformed active summaries remain invalid.
- Compare all human option/comment signals before/after (content and status, with explicit accounting for structural title context if heading form affects attachment). Preserve checked Done exactly; do not manufacture a submission or clear one.
- Assert hand-back and pickup agree on remaining shared shapeless defects. Retain fenced/nested false-heading, malformed-summary, internal-field, and archive-checkbox tests.

Parent live sequence: preserve private fresh pre-edit bytes; anchored hierarchy-only edit under existing authority; fresh readback; verify exact intended structural delta, all human signals, parser and hand-back; then installed pickup qualification on the unchanged unchecked submission state. No builder live edits or peer dispatch required. Keep registration paused until those checks pass. This report authorizes no additional action itself.

Authority clarification from bounded source evidence: docs/specs/2026-09-22-decisions-current.md explicitly says “No deletion of the page's Night log or Goals-ruling sections (owner's call, recorded on the page as a note from the lead, not a decision item).” Its page-rule paragraph likewise says extra sections are not deleted by that build. Neither passage prohibits changing heading levels while preserving the sections and every content byte. The hand-back comment's “keep ... as-is” is a broader implementation gloss; this bounded review found no direct user instruction requiring unchanged hierarchy. Under the parent's confirmed current page-edit authority, no additional permission gate is warranted for the specified hierarchy-only normalization. This does not establish permission to delete, paraphrase or move human inputs.

Minimal hand-back contract delta: replace the old test that admits arbitrary kept H1 sections with two paired assertions: (1) unknown H1 outside Closed + optionless summary => SHAPE and HANDBACK blocked, nonzero under enabled enforcement; (2) identical historical text beneath H2 within Closed => no SHAPE and HANDBACK ok when all other existing conditions are clean. Keep existing active-Waiting malformed and no-heading fail-closed tests. These directly discriminate removal of the redundant permissive filter, without broadening parser scope or adding a second classification engine.
