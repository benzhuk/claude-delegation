VERDICT: APPROVE
Spec reviewed at875f195: docs/specs/2026-09-24-decisions-archive-scope.md. Read-only review of decisions-read.mjs and pickup invalid guards; no tests, live calls, page reads or source changes performed.

The shared-reader change fixes the cause. Current shapeless construction includes every optionless summary; pickup correctly rejects warnings/shapeless before accepting an unchecked page. Moving that pickup guard below Done or suppressing archive errors only in pickup would leave hand-back callers inconsistent and allow malformed active pages through. Retain the pickup guard unchanged.

Required implementation boundaries already implied by the spec, with concrete adversarial cases for the independent contract lane:

1. Narrow recognition: recognize only the specified unindented level-one Closed heading, including the existing toggle="true" attribute form. Do not call normalizeTitle blindly: it strips numeric parenthetical suffixes, so '# Closed (8)' could incorrectly activate archive scope. Preserve exact-title intent: reject Closed history, Not Closed, Closed (8), lower-case variants, ## Closed, indented/list/quoted Closed. Define any supported bold wrapping explicitly through the existing visible-title contract; do not infer arbitrary Markdown rendering.

2. Nested/fenced headings: a column-zero '# Closed' inside a details block is structurally nested despite its indentation. The spec explicitly excludes nested headings; test this separately from indented headings so a column-zero regex alone cannot silently satisfy that requirement. Keep the existing backtick-fence semantics; do not broaden this repair into a general Markdown parser. A fake/fenced Closed must not activate scope, and a heading inside a fence must not end real archive scope. An unclosed existing supported fence and malformed summary remain BLIND even inside Closed.

3. Record scope at each summary's creation, not from the final page-level scope. Test active shapeless -> Closed -> archived summary -> new level-one active section -> active shapeless. Only the archived one is omitted. No mutation of currentTitle attachment semantics merely to track archive state: comments/ticks use the existing nearest-title rules. Archive scope is not permission to discard whole blocks.

4. Preserve every signal: archived optionless summary plus escaped human comment remains UNATTACHED, including after Reply text; archived summary with unchecked options remains a decision and receives missing-default warnings; with a checked option remains TICKED, two checked options AMBIGUOUS. Duplicate/indented/nonterminal Done handling must remain unchanged. Test comments/ticks directly beneath Closed as well as under nested summaries, and after the archive's next top-level heading. A malformed active toggle must still make actual pickup INVALID even if Done is unchecked.

5. API shape: current parseDocument decision mapping spreads title properties into public decisions after stripping selected internals. An enumerable archive flag will leak unless removed. Prefer private side metadata (e.g. Set of title identities) or explicitly strip the marker. Assert exact keys on parseDocument decisions and the top-level result, not merely CLI JSON (toJsonObject reconstructs fields and could hide the leak). No new export, serialized field, state store or parser engine is needed.

Contract adequacy: the proposed actual pickupOnce unchecked-page and checked-current-selection-plus-archive cases establish caller usefulness rather than parser-only success. Require zero capture/send for unchecked, exactly the intended current selection capture/send for checked, and a separate archived-human-comment preservation case. Use synthetic titles/comments only. Preserve the0.20.5 reproducer; private real-page parsing remains parent-owned evidence and is not permission to publish history or run pickup/send.

No spec-blocking change requested. Implementation acceptance remains conditional on these existing constraints; specifically, treating all column-zero text as structurally top-level or reusing numeric-suffix normalization would violate the approved spec. The eight historical summaries must remain untouched on the live page. Registration stays paused until parent installation/qualification, without manufacturing checked human input.
