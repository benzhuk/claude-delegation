VERDICT: APPROVE — bounded installed Claude skill-use qualification on Hetzner and Netcup, delegation 0.20.4; first responses failed the word limit, native resumed corrections passed.

Scope: September 24, 2026 (America/New_York). Actual authenticated provider work through existing Orca remote environments, ordinary installed Claude home/config/hooks. Private temporary working directories, Read-only agent tool allowlist, no auth/settings changes, plugin updates, live decisions/Notion reads, peer sends, or source edits. No project tests or suites. This is source-grounded operational writing, not execution of pickup/accounting, continuation enforcement, peer delivery, unattended scheduling, every skill, or interactive TUI qualification.

Both hosts: Claude Code 2.1.281; native init reported delegation@benzhuk 0.20.4 and all nine delegation skills, model claude-sonnet-5, firstParty provider/list cost basis. Each initial run made exactly one Read call to /home/ben/.claude/plugins/cache/benzhuk/delegation/0.20.4/skills/decisions/SKILL.md. Its tool_result was non-error and contained the One-shot pickup and owner accounting section. Installed file SHA256 on both hosts matched the independently read source: 7c4a06e0b4a14b231c13161b104c905720df43b6686a24fa211482cdb6b0d3ad.

Each initial run observed hook_started/hook_response pairs for SessionStart (4), UserPromptSubmit (4), PreToolUse (2), PostToolUse (4), PostToolBatch (1), Stop (3). All hook_response exit codes were zero. These counts describe the normal installed runtime collectively; they do not prove each event belongs exclusively to delegation or that a substantive enforcement branch was exercised. Each resumed correction observed SessionStart (4), UserPromptSubmit (4), Stop (3), all zero, and no tool calls. Original Read runs completed in two model turns; corrections completed in one. No permission denials or auth failures were reported.

Original negative evidence is retained unchanged: Hetzner 138 whitespace-delimited words; Netcup 121. Their operational content was substantially grounded, but they were not accepted as constraint-compliant. One correction per host resumed the SAME native session by session_id, using existing context; no root-edited text is represented as native acceptance. Final notes correctly separate configured host/page/project exclusivity, lack of unattended scheduling, routing, authority, and attended accounting/fresh reconciliation. Native prose summarizes rather than exhaustively describing every refusal/reconciliation state.

Hetzner (zhuk-vps32)

Original wall time 13.812s; correction wall time 7.894s. Initial native duration 12456ms; correction native duration 5504ms. Session cumulative provider list-cost estimate USD 0.1945034 (initial USD 0.181806; do not add those cumulative figures). Cumulative model usage: input 6, output 972, cache-read 43737, cache-created 44006. Both exit 0. Final word count 90.

One-shot pickup (`decisions-pickup.mjs --once`) is not an unattended scheduler — it's one bounded read invoked explicitly on a single configured host, bound exclusively to one registered page/project; a second project or worktree registering that page is `PENDING_MANUAL_HANDOFF`, never an independent round. It only routes: capturing a checked Done into private receipts, never editing the page or clearing Done, and a checked Done grants no authority by itself. Authorization is separate — the owner must handle each captured selection/comment against a fresh reconciled read, then `decisions-pickup.mjs account` records that attended attestation.

Private host receipt directory: /tmp/delegation-native-use-0924-2w3wf3fq (mode 0700). Original native.stdout.jsonl, native.stderr.txt, summary.json and separate correction.stdout.jsonl, correction.stderr.txt, correction-summary.json preserved. Local summaries/terminal captures: 0924-Hetzner-summary.json, 0924-Hetzner-terminal.json, 0924-Hetzner-correction-summary.json, 0924-Hetzner-correction-terminal.json, all beside this report.

Netcup (v2202608391056492408)

Original wall time 9.703s; correction wall time 5.129s. Initial native duration 7442ms; correction native duration 2856ms. Session cumulative provider list-cost estimate USD 0.1237702 (initial USD 0.112197; do not add those cumulative figures). Cumulative model usage: input 6, output 694, cache-read 64131, cache-created 25998. Both exit 0. Final word count 89.

One-shot pickup (0.20.4) is not an unattended scheduler: it's a single bounded, manually invoked read (`decisions-pickup.mjs --once`) on one explicitly configured host, exclusively bound to one registered page and authorization project — a second project/worktree gets `PENDING_MANUAL_HANDOFF`, never an independent round. Pickup only routes a captured checked Done into a private receipt; a checked Done grants no authority by itself, and routing is not authorization. Actual authorization is separate: an attended session must reconcile a fresh page read, handle each capture, and run `decisions-pickup.mjs account` with an owner attestation.

Private host receipt directory: /tmp/delegation-native-use-0924-wcjzn3ct (mode 0700). Original native.stdout.jsonl, native.stderr.txt, summary.json and separate correction.stdout.jsonl, correction.stderr.txt, correction-summary.json preserved. Local summaries/terminal captures: 0924-Netcup-summary.json, 0924-Netcup-terminal.json, 0924-Netcup-correction-summary.json, 0924-Netcup-correction-terminal.json, all beside this report.

Combined cumulative provider list-cost estimate: USD 0.3182736; not a billing invoice. Native API duration on resumed results is cumulative; wall times above were measured separately for each subprocess. Private runners are 0924-linux-native-run.py and 0924-linux-correction-run.py beside this report. Process timeouts were bounded and unused.

Both task-owned Orca terminals were closed successfully after the commands completed; 0924-Hetzner-close.json and 0924-Netcup-close.json record ptyKilled:true for the owned shell only. No pre-existing terminal was operated or closed. Orca routes are operational but old-host terminal-send acknowledgments do not provide durable submission receipts; completed native JSON results and terminal output supply actual execution evidence here. No update was attempted.
