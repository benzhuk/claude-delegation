VERDICT: NEEDS_FIXES a2dc608

Parent contract triage, not independent approval. Builder PASS is rejected: the implementation does not meet the supplied spec or its stated purpose.

Cause: a wrapper around temporary absolute-path fixtures was substituted for a portable source runner. The wrapper preserves dependency on the exact ephemeral session artifacts it was supposed to replace, and substitutes metadata assertions for actual invocation identity.

Discriminating check: scripts/native-continuation-smoke.mjs hardcodes C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923; without that directory it cannot run. --claude only prepends PATH and never invokes the supplied executable directly, so the recorded basename is not proof of the executable used. Both temp fixtures contain hardcoded checkout paths. The runner inherits all process.env into its child driver, lacks its own timeout/spawn-error handling, only runs accounted plus interrupt scenarios, and still derives account revision from private fixture state. Its current temp drivers use a compact system prompt, so it does not preserve the release-blocking DEFAULT-system-prompt regression. A false-positive context presence assertion is not model-visible-argument sourcing.

Fix location: replace the wrapper with a self-contained source runner (within the two owned files). Copy/adapt the corrected protocol driver code into the actual shipped script and factor one server/process routine. Resolve plugin root from import.meta.url; no dependency on temp source artifacts, local checkout names or Ben's paths. Invoke the exact absolute --claude executable. Allowlist child env before any subprocess, set all disposable homes explicitly. Native --version observation belongs in summary. Bound child/server lifetime and handle spawn failure. Test all three scenarios: DEFAULT prompt unaccounted->oneblock/refire, accounted silence, native interrupt and replacement STALE_EPOCH. Parse native binding identity/epoch from model-visible hook context; account revision from successful bind toolresult or actual status tool. Private state reads are assertions after execution only. Reject invalid flags/output reuse. Preserve failed outputs; no destructive cleanup of caller paths.

Simplification: one portable opt-in source script with shared fixture server/process code, not a shell around a second ephemeral implementation. No production runtime or regular-suite dependency.

Verification before reporting delivery: temporarily choose a clean unrelated cwd and run using the absolute source script path plus exact executable, without invoking any review-adapter-*.mjs files. Assert the default-prompt case actually crosses 64KiB and succeeds. Report real clock tool timestamp, not inferred or machine-mismatched time. Include source-only node --check, bad-argument check, actual complete smoke summary, and limitations. A syntax pass or subprocess exit is insufficient. Do not claim PASS while any named requirement is knowingly unsatisfied; continue fixing rather than ending at partial wrapper.
