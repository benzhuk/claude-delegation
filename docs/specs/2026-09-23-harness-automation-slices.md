# Independent automation source slices

Authority: Ben requests progress on the whole harness in maximum useful parallelism and approved merges. Source, tests, review and merge are authorized. No installation, schedule activation, live note sending or knowledge publication by builders. Automatic daily assessments, full recovery and two-provider parity are not established by these source slices.

Inputs: followon-design-review.md and source scouts in this directory. Its corrected contracts below override scout suggestions. No new controller, database, scheduler or note transport rewrite. Source tests use sealed disposable homes. Existing A/B/C delivery gate does not wait for this work.

## D — due/completed bearings through existing hook

Own skills/bearings/scripts/bearings-state.mjs (new) and test, hooks/delegation-reminder.js and existing tests, skills/bearings/SKILL.md and evidence-template.md. No Codex hook edits or hooks.json registrations. Canonical helper is inside the mirrored skill so callable commands do not depend on a checkout-only scripts path. Can import existing single config loader from ../../decisions/scripts/project-config.mjs; both are packaged plugin skills. No duplicated project parser. Hook imports this helper by known plugin-relative path. Missing dependency never claims completion; hook fails open preserving all existing output.

Pinned CLI: node <bearings-skill>/scripts/bearings-state.mjs check --repo <project-root>; complete --repo <project-root> --report <path> --lead-response <path> --publication <https-url>. Module import must not run CLI. Check prints JSON with status due/current/unconfigured/unknown/disabled and reason. Check and complete use current goal card at docs/goals/card.md; explicit project root identifies this initial scope. No invented cross-worktree identity: canonical real project root keys sha256 under AGENTS_HOME/ws/bearings. For hook cwd project resolution use existing config loader; do not key by common Git directory or every HEAD.

Completion receipt is a small version1 JSON: projectRoot, goalPath, goalDigest, completedAt (ISO), reportPath/reportDigest, leadResponsePath/leadResponseDigest, publication. complete validates readable nonempty files, current goal, valid publication URL and writes atomically; recording is explicit caller attestation of completed independent assessment, lead response and verified required publication, not mechanical proof those contents are true. Check rechecks identity/digests and elapsed24h; missing, corrupt, future-dated, unsupportedversion, changedgoal, or missing/tampered evidence cannot suppress due. Missing goal is unconfigured (not completed), unreadable goal/state is unknown (not current). Receipt outside plugin caches. No new report headers or schema imposed on narrative evidence.

Only Claude automatic notice is supported in this slice. Extend existing SessionStart and supported periodic PostToolBatch path. Bearings positive nonempty agent_id child exclusion occurs BEFORE bearings I/O, not before existing child goal-card handling. Respect ws-off, ws-off-goalcard and new ws-off-bearings; uncertain switch reads disable. Completion state separate from per-session notice throttle. SessionStart always reconsiders still-due work; periodic notices bounded using current session identifiers/existing per-session paths, no global once-per-day suppression after abandoned session. Notice says due/unknown rather than completed; publication-pending assessment is reused per skill instead of automatically rerunning fresh assessment. Do not perform network/LLM calls in hooks. Existing hook bounded exit0 and output remain valid, no second response object. No new scheduler and no idle-host assessment promise.

Release and KILL stay explicit bearings workflow invocations described in skill with reference to the release/condition in assessment; no HEAD-based automatic release detector. complete updates only after required publication/lead accounting. Tests discriminate notices from completion, real elapsed24h, future/tampered/missing receipts, project separation, child no-bearings-write, switches, preservedgoalcardbehavior and periodic assessment due. Synthetic actual copied plugin skill invocation verifies helper dependency and no livehome writes. Instruction-only checks remain labelled.

## E — callable one-shot pickup with recoverable dispatch receipt

Own new skills/decisions/scripts/decisions-pickup.mjs and test; decisions SKILL.md for accounting/status/resume instructions only. B's config repair is prerequisite and integrated. No parser or note-send core changes unless parent adjudicates concrete defect. One registered page per explicit invocation, one pickup host, exclusive per-page claim; no automatic stale-lock breaking while prior sender may still run.

Pinned CLI: --once --page <id> --repo <root> --from <slug> --owner <slug> --reader <notion-cli-path>; status --page <id> --repo <root>; account --page <id> --repo <root> --outcome <existing-report-path>. Source exports injectable read/send/fs/time seams for failure tests. --once invokes configured existing notion reader boundedly for page, uses existing parseDocument, and sends via existing runNoteSend. A supplied fixture export is a test seam only, not the production automatic reader. No network page mutations. Unchecked/unchanged pages use zero model turns or sends. Done grants no authority.

Use existing notes/packet locations for immutable original bytes/page/read-time/owner capture; one small local per-page receipt under AGENTS_HOME/ws/decisions-pickup. Receipt version1 includes page,project,round,capturePath,digest,owner,from,noteId,exactsendinputs,state,accountingOutcome. Capture hash is integrity, not round authorization. Save capture and PREPARED before dispatch, save SENDING before runNoteSend. Use preexisting capture with --details, never --packet-file or --force. PREPARED resume may send saved ID once. SENDING/uncertain resumes inspect positive exact transport evidence; matching existing envelope -> RECORDED, conflicting -> reconciliation, absent/unreadable -> UNKNOWN and no resend (do not trust swallowing readLedgerCorpus). RECORDED never appends again; transport repair/normal inbox pickup handles wake separately. An orphan capture remains visible/reconciled, not treated as sent. All uncertain states visible via status.

One active round per page. Changed checked bytes are later edits to that round, preserved and marked needs-reconciliation, not a new submission or authority. account requires readable owner outcome referring to every captured selection/comment and fresh-page reconciliation; command records explicit owner attestation and reference, not a claim of mechanically verified consequences. Pickup never clears Done. The existing attended owner handles current content and clearing. After accounted, observe an unchecked valid page before another checked episode can admit a new round. Invisible same-byte uncheck/recheck between reads is unsupported. Owner absent remains waiting-owner; owner replacement must explicitly bind existing round without redispatch or repeating outcomes; if no safe rebind implemented, expose explicit pending manual handoff, not automatic resurrection. Account cannot make uncertain send safe to repeat.

Tests inject crashes at capture/PREPARED/SENDING/send/RECORDED boundaries, concurrent calls, uncertain transport, conflictingnote, unreadablelookup, changedcheckedcontent, noowner, account prerequisite, observedunchecked reset, and zero repeated send. State is bounded dispatch/accounting metadata, not a mirrored mutable decisions page or universal state engine. Initially bounded manual reconciliation for UNKNOWN and attended clearing are explicit limits. Full unattended owner recovery remains a real backlog requirement. No live sends or schedules activated by build.

## Verification and roles

Read final spec and scouts; honor territory ownership. Builders commit source only, parent owns work records, shared release metadata and publication. High-tier integrity builder for E; mid-tier D implementation plus independent high-tier review. At most three light focused sealed tests simultaneously, full suite exclusively integrator. Report actualSHA/verdict/tests/unknowns, state path and cleanup. No childagents or fullsuite without parentgrant. All source changes receive independent probes/review before integration. New work records explicitly note the C/B prerequisites already met locally so they need not wait for unrelated A gate.
