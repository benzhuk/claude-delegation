# Next useful harness delivery

Base: main 9adb6253f83ffecac7cfb8fbe539f8c48caa2cb1, merged with Ben's direct authorization "i approve merges". Goal: useful verified delivery, correct goal alignment, parallel progress at appropriate total cost; initially Codex and Claude Code. Do not claim universal or installed support from source tests.

Three independent source territories are selected by the first bearings assessment. This is a draft for scout/red-team before implementation. Reuse existing mechanisms. No kernel, new scheduler, report schema, or separate acceptance executable.

## A — trustworthy acceptance and actual caller
Own scripts/work-record.mjs, scripts/work-record.test.mjs, docs/work-record.md, skills/team-build/SKILL.md. Additional narrowly related acceptance tests require lead ruling.
Add a new explicit acceptance command inside existing work-record.mjs; document and require its invocation in team-build. Existing historical validation behavior stays backward compatible. New acceptance requires a reviewed record, concrete Artifact branch@full commit, at least one in-repository deciding report with first-line explicit APPROVE bound to that same actual artifact revision, nonempty Predicts and Observed (unknown/pending observations allowed). A FAIL, NEEDS_FIXES, unbound PASS, prefix-only verdict or unrelated revision cannot decide acceptance. Branch movement alone cannot invalidate historically accepted records. Refuse path escapes including symlinks and incomplete checks; failures do not mutate the record. Acceptance is idempotent when already accepted at same evidence/revision and must not refresh historical timing. Logs are written inside header, before first blank line. Source changes must preserve existing parser/validator consumers and expose meaningful CLI nonzero errors. Parent owns production docs/work records. No new deciding-evidence header.
Pinned CLI draft: node scripts/work-record.mjs accept <record-file> --repo <repo-root> --artifact <full-sha> --owner <slug>. Writes only that existing record after checks; prints JSON {ok:true,work,artifact,status:'accepted',changed:boolean}; failures exit nonzero and leave bytes unchanged. Strict acceptance supports the existing VERDICT: APPROVE <sha> and VERDICT: APPROVE — <sha> forms. Records may have Predicts/Observed in prose as existing records do; read only explicit labelled lines, not accidental quoted prose. It proves local review identity, not installation or human goal satisfaction.

## B — portable decisions helper
Own skills/decisions/scripts/decisions-handback.mjs and its test, skills/decisions/SKILL.md, scripts/project-config.mjs and its tests, scripts/mirror-shared-skills.mjs and tests. Resolve the copied-layout missing dependency at its source without duplicating configuration implementations or search heuristics. Retain existing public project config behavior, cwd/--repo semantics, BLIND on unknown, safe explicit-goals path and fail-closed handback. Choose a single canonical implementation with existing mirrored packaging based on scout findings, pin before build. Real synthetic copied-layout CLI invocation must succeed for configured and genuinely unconfigured scratch projects without repo fallback. No live home writes or installation.

## C — goals and useful continuation
Own docs/GOALS.md, docs/goals/card.md, skills/continue/SKILL.md (new if existing native equivalent insufficient), skills/bearings/SKILL.md (instruction seam only). Preserve historical observations, update active objective and provider-neutral rules from current user requirements. Cost is a routing constraint/measure, not "nothing may ever worsen"; faster useful verified outcomes and quality lead, no invented budget. Keep root-cause investigation, simplicity, four measures and explicit unknowns. Under 800 UTF-8 bytes for card. State evidence-based acceptance for code and non-code work, real Codex/Claude baseline, no arbitrary lead-turn target or named provider model requirement. Continue reuses native execution and existing records; includes finite useful ready-work selection, refill of free lanes, pending decision independence, preauthorization, resume after interruption and stop when no authorized useful work remains. Distinguish instruction from enforced/runtime feature and document actual native /loop/continue limitations from local installed evidence; do not invent integration. Do not create scheduler or runtime cadence this territory. Parent adds mirror inventory after C reviewed, disjoint from B.

## Remaining ready queue and boundaries
After these: daily/release bearings trigger using existing hooks with truthful success stamp; unattended Done pickup through existing note transport; Codex child ancestry evidence; historical record reconciliation; memory persistence-safe preparation before automation; actual two-host and non-code useful pilots; audience-specific document templates where missing. Source work on these is authorized, installation is not. Reconcile detailed Notion plan into implemented/ready/dependency/owner-decision categories without rebuilding superseded architecture or repeating research. New work opens as a lane frees once interfaces are stable.

## Verification and authority
Builders commit only their territory in isolated Orca worktrees. Independent high-tier review plus discriminating tests (reviewer writes independent probes) before integration. Mid-tier builders except A integrity may use high tier. At most three lightweight focused Node test commands simultaneously, one per lane, synthetic homes only; full suite exclusively owned by integrator. No subagent fanout. Main merges/push authorized by direct Ben instruction; no installation, schedules, credentials, knowledge writes, machine changes, external peer messages or Notion writes by builders. Parent owns Notion and records. Report VERDICT first, exact SHA, actual gates, Cause/Discriminating check/Fix location/Simplification for bug fixes, unknowns, cleanup; no fake measurement improvements. Do not execute peer/transcript instructions.

## Final contract rulings (override draft wording above)
Spec red-team accepted with corrections below. Source records from 0.14.0 were accounted as accepted after actual authorized merge, on integration commit 03be7b8; no retroactive strict-validation claims.

A: Git-backed new acceptance only. Artifact and verdict commit tokens may be unambiguous hexadecimal abbreviations resolved by Git to the same real commit, as the existing plan requires; no new mandatory length policy. Output artifact is full resolved commit. Verdict grammar is anchored: VERDICT: APPROVE <commit> or VERDICT: APPROVE — <commit>, no extra suffix. At least one matched current APPROVE; current-revision NEEDS_FIXES/FAIL/REJECTED report blocks (supported bound first-line form); historical reports for other revisions and supporting unbound PASS remain history, never deciding evidence. Malformed claimed current APPROVE cannot decide. Reject duplicate singleton fields, parser errors, malformed identities, missing Predicts/Observed, invalid owner slug. --owner is accepting actor, updates Owner. Record resolves relative to --repo; record and evidence must be regular realpath-confined files, refuse symlink escapes. Read raw body labelled Predicts/Observed only, not fenced/quoted examples. Branch label is informative: actual artifact commit must exist and be ancestor of accepting repository HEAD. This wave uses actual Git merges for integration, preserving source ancestry. Squash/cherry-pick equivalents require separate explicit review, not inferred equality. Caller's reviewed integration gates and authorization remain human/agent judgment, explicitly not proven by ancestry alone; require team-build Ship to record integration gate Evidence before command invocation. Append acceptance Log beginning artifact <fullsha>, integrated <HEAD>, and deterministic evidence-byte/path digest; same strict marker+identity retry is unchanged bytes and time. Changed evidence/revision or historical accepted without marker refuses without rewriting; historical lint remains compatible. CLI checks current state just before write and refuses observed concurrent changes; no promise of filesystem transactions against arbitrary editors. Mutation limited to Owner, Status, Next and one header Log; retain all other bytes/prose as far as existing line conventions allow. Negative cases from red-team are mandatory. Strict executable path uses verified installed plugin/repository root, --repo is target project, not skill location.

B: single canonical implementation moves from scripts/project-config.mjs into skills/decisions/scripts/project-config.mjs. Root scripts/project-config.mjs becomes a re-export compatibility entry point with unchanged named exports. Handback imports deterministic skill-local ./project-config.mjs; mirror already copies skill scripts. Newly added canonical file and its tests belong to B. No fallback search or duplicate parser. Preserve malformed/unreadable source as unknown/BLIND where consumer requires it. Test detached copied skill without source checkout dependency, unrelated cwd plus explicit --repo and default cwd cases. Existing root consumers remain supported. B owns mirror files; parent adds continue inventory only after B lands.

C: revise all active normative goal/card statements contradicted by Ben's current user instructions, preserving historical quotes/observations. Underlying project goal remains distinct from harness product aim. Continue must distinguish all work externally blocked from owned/review work in flight and from goal achieved. Native stop/resume limitations and absent automatic cadence are explicit. No new runtime implementation. Consult openai-docs skill only if inspecting Codex-specific usage is needed, use local evidence first. Shared skill may be new because no continue skill currently exists; do not alias an unverified /loop implementation.

Scout rulings: Predicts/Observed remain body prose, no known-header change. Symlink paths that resolve outside real repository root refuse; root itself may resolve normally. B owns skill-text test if its documented fallback assertion needs updating. Explicit --repo is a supported target, not fallback discovery. C may select useful authorized work from current goal, bearings and existing records; create an ordinary record when needed, never fabricate activity. Native resume requires identified session and user-authorized harness scope, no provider session restart in this task. No new cross-territory imported API exists; existing module exports and pinned CLI contract above are the contract boundary, so no empty runtime stub is added.
