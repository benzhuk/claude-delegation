# L1 brief — the loop script

Mandate template: docs/mandate-template.md (plugin repo). Spec: /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/../2026-09-25-build-loop-workflow.md
(Territory L1). Pinned contracts (lead's rulings, WIN over the spec): /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/contracts.md — R1 through R9.

Worktree: /home/ben/Code/wt-one-launch-L1 (branch build/one-launch-1-L1, cut from fbd7cf6).
Files you own: skills/team-build/references/build-loop-workflow.js, build-loop-workflow.test.mjs,
build-loop-args.example.json (plus an optional build-loop-args.legacy.example.json), scout-brief.md.
Touch nothing else.

Scout first (you are your own scout this build): read the current script, its test harness and
skills/delegate/references/ladder-workflow.test.mjs for the harness precedent, before changing anything.

What to build:
1. Setup stage (R3), Seam stage (R4), Accept-prep stage (R5), startFrom (R6), return shape (R7),
   arg validation (R2: mixed-territory-modes, missing-args). Keep every existing behaviour of the
   given-worktree path byte-for-byte where the contract does not change it (sameSha, longerSha,
   the round loop, the respawn-once-on-death rule).
2. Add phases Setup, Seam, Accept to meta (pure literal).
3. Header comment rewritten to describe the new args/return and the one-launch flow in <= 45 lines.
4. Tests (extend the existing static harness, same AsyncFunction pattern; no real agents):
   - static contract for the new args, phases and schemas;
   - a fixture run of the SETUP path with a scripted fake agent() that returns per label, proving one
     launch produces: setup worktrees/briefs, builds, reviews, integrate, seam, accept-prep with a
     censusPath and a checkAcceptance result — and a fixture JOURNAL (array of every agent() call the
     fake saw, plus one entry for the script's single return) where the count of script returns is
     exactly 1 and no call's agentType is anything the pane would issue (pin: all calls originate inside
     the script; the journal has exactly one 'return' entry);
   - the given-worktree path still returns seam:null, acceptance:null when integrationWorktree is absent;
   - setup-failed on a wrong worktree/branch/headSha from the fake setup agent;
   - startFrom APPROVE skips build+review; startFrom NEEDS_FIXES starts at a fix round;
   - seam NEEDS_FIXES -> seam-fix -> seam APPROVE; seam rounds-exhausted;
   - accept-prep is skipped (acceptance.skipped) when seam is not APPROVE/SKIPPED;
   - no rendered prompt contains a note-send instruction other than the prohibition (R9).
5. build-loop-args.example.json becomes the new one-launch shape (setup territories, leadSession,
   recordPath, integrationWorktree/Branch/Gate); keep the old shape as build-loop-args.legacy.example.json
   and have the test parse both.

Gate: node --test skills/team-build/references/build-loop-workflow.test.mjs (then
node scripts/run-tests.mjs skills/team-build/references/build-loop-workflow.test.mjs if that runner takes
a file list). Commit early and often, conventional commits, no trailers.
Report to /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/reports/L1-report.md, first line
VERDICT: PASS|FAIL|BLOCKED. State file: .../reports/L1-state.md.
Never set or switch a git identity; never push; no destructive git; never send peer notes.
