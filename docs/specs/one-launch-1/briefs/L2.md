# L2 brief — the skill

Mandate template: docs/mandate-template.md. Spec: /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/../2026-09-25-build-loop-workflow.md (Territory L2).
Pinned contracts: /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/contracts.md (the script's args/return/stages are EXACTLY as pinned there; L1 is
being built concurrently against the same file — describe the contract, not L1's code).

Worktree: /home/ben/Code/wt-one-launch-L2 (branch build/one-launch-1-L2, cut from fbd7cf6).
File you own: skills/team-build/SKILL.md. Touch nothing else.

What to write:
1. Rewrite "Running the loop from an Opus pane" as the DEFAULT way to run a build with 2+ territories:
   two lead turns — launch and accept. Launch turn: spec pack on disk (spec + contracts + territory
   map), base sha, lead session id (R8: from the hook context line "Host: claude; session: <uuid>"),
   startedAt, open the record (recordPath), then one Workflow call with the args of contracts.md R2.
   What the return contains (R7). Accept turn: read the return; if acceptance.checkAcceptance PASS,
   run "work-record.mjs accept ... --census <censusPath>" (or --no-census "<reason>" when censusPath is
   null) and send ONE RESULT; otherwise the named blocker decides the one next step.
2. One sentence each: three-wakes rule (a lead wakes its requester at most three times: ACK, RESULT,
   BLOCKED); one-notification rule (a build's fix rounds, re-reviews, seam, integration and census run
   inside the one Workflow call, so the lead gets one notification); census-at-accept rule (fresh census
   after the last review, lead file plus subagents dirs) — written conditionally: "when the record
   supports --four-read, pass it at accept".
3. startFrom (R6) in one short paragraph: how to resume a half-finished build without rebuilding.
4. Codex paragraph (R10): no Workflow tool; the manual sequence (Setup through Ship) in the same order;
   same census definitions; record says so; no emulation.
5. CUT what the new flow makes redundant: the old "Pre-launch steps, in order — the script does none of
   these itself" list, the Setup section's scout/worktree/record steps where the launch now does them
   (keep them as the Codex/manual path, stated once), duplicated census instructions between Ship and the
   loop section, the "This section is the file's last section" meta-note. Fewer lines than 371 is the
   expected shape; report the before/after line count.
6. Keep every rule that is not about the loop (roles, tiers, JUDGMENT: lines, accept semantics, bug-fix
   fields). Do not edit README.md (the RESULT carries the changelog line).

Gate: node --test skills/team-build/ (any test that reads SKILL.md, e.g. grep the repo for tests that
readFileSync SKILL.md: run those) — list what you ran. Commit early, conventional commits, no trailers.
Report to /home/ben/Code/claude-delegation-lane4/docs/specs/one-launch-1/reports/L2-report.md, first line
VERDICT: PASS|FAIL|BLOCKED; include the line delta. State file: .../reports/L2-state.md.
Never set or switch a git identity; never push; no destructive git; never send peer notes.
