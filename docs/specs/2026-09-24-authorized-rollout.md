# Put the released harness to work

Authority: on September24 Ben selected all three existing Notion decisions and then confirmed in chat, "Decisions made. Make the report but don't wait on the fix, might take months." Canonical page: https://app.notion.com/p/3e1da11277a18174bccfea187d5c3972 . Fresh attended reader returned three TICKED items; Done was still unchecked. Direct chat confirmation independently submits those selections. Preserve exact page bytes privately, not in this repository.

Approved actions: install the released plugin and its existing shared skills/roles/hooks on configured machines with bounded live validation; report unreachable or unverified hosts. Keep the existing knowledge store and allow a designated writer standing knowledge-only publication after durability checks: curated topic/index/digest only, visible digest, no unrelated paths, raw sessions, secrets or unreviewed inbox publication. Publish the reviewed Codex hook-shell report after duplicate search. Do not wait for upstream repair to continue useful work.

Execution boundaries: preserve unrelated work/config and existing auth; no credentials copied, identity changed, policy bypass or automatic triage/schedule enablement. For each host inspect existing route and choose one hook route to avoid duplicate execution. Use supported CLI installation, targeted merges and backups. Native Claude and existing Codex mirror are valid initial routes; replacing a working route just for symmetry is not required. A selected Windows shell may be qualified in an invocation-specific supported launch without global PATH changes or per-hook patches; retain an explicit limitation if host launch selection cannot be made reliable.

Parallel territories: parent owns source integration/release, Notion accounting, issue publication and Windows rollout; mid-tier agent prepares remote-host inventory/commands read-only; high-tier agent scopes memory durability at the existing forced-update boundary. Each build is separately admitted and independently reviewed before rollout. Source diagnostics are subordinate to real installed useful work.

Outcome evidence: per-host installed version/source identity, chosen hook route, actual discovery/handler execution, useful artifact and acceptance where possible. Memory requires preservation of unpublished edits across ordinary forced-update behavior and named-file readback on a second host; a new synthetic unreviewed inbox file cannot satisfy the curated-only publication grant. Whole-task speed/cost and later rework remain unknown until measured with proper boundaries.
