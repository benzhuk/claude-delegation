# First useful harness build wave — Astra

Base: main 78cb46e795b252a74e878be4aa39970b12878ce7. Read current source, not stale integrate/0921 checkout. Goal: usable agent-agnostic work harness, starting Codex and Claude, faster accepted deliverables and measured quality through simpler underlying mechanisms.

Authority: isolated source edits, local commits and tests, independent review, integration candidate preparation. No main merge, push, installed config mutation, scheduling, live publication smoke or rollout. Preserve unrelated work. No git identity changes. Owner is skills-a, not peer advisors.

Inputs: C:/Users/benzh/AppData/Local/Temp/astra-feedback-0923/revised-detail.md is the adjudicated plan. build-review.md and docs-review.md in that directory contain source evidence. Do not repeat previous research. Preserve existing APIs unless this spec explicitly changes them.

## Disjoint territories

D: new skills/bearings/SKILL.md and skills/bearings/references/evidence-template.md only. A small callable skill, using existing evidence and native high-tier delegation, no new runtime or scheduler. Reproduce the four user questions and CONTINUE/RE-PLAN/CUT behavior from revised D. Both hosts use the same instructions. Clear bounded review input, truthful unknowns, own-words lead response, required dated Notion publication with linked repository evidence and pending status on failure. Two ineffective RE-PLANs trigger reassessment, not automatic user goal changes. Read skill-creator SKILL.md at C:/Users/benzh/AppData/Roaming/orca/codex-accounts/f8bc0bab-fa9c-4317-b296-797e4dc50024/home/skills/.system/skill-creator/SKILL.md before authoring. No executable tests mirroring prose.

A: skills/team-build/references/build-loop-workflow.js and its existing test file only. Replace literal prior sha with captured previous builder SHA before new build overwrite. Both reviews must match the current artifact. Require explicit approval at integration. Normalize null/omitted parallel returns by planned position so a failed territory cannot erase successful results. Preserve existing output contract except additive failure details. Independent behavior tests must reproduce defect at base. Do not implement work-record acceptance changes in this wave.

G: skills/delegate/references/ladder-workflow.js and its existing test file, plus skills/delegate/SKILL.md only. Default nonempty budget 2*N+1, insufficient explicit cap rejected before dispatch, empty input zero-call inconclusive. Flat positional coverage retains all targets including duplicates. Failed reader still gets research. Null/missing research and access-failure prose never treated as coverage. Judge sees coverage and limitations. Preserve verdict/evidence/cost shape, add coverage rows { index, target, read: {status, reason}, research: {status, reason}, sources: [] }; statuses complete/unavailable/unverified/not-run. Complete requires inspected reference and attributable finding, not arbitrary nonempty text. Prompt stages for a compact structured result, accept legacy free text as unverified. Do not claim model declarations prove access. Errors retain required/provided budgets in their message, no reliance on custom error fields surviving runtime. No filesystem globals. Fold practical delta research guidance into existing delegate, not a new research skill. Exact existing runtime constraints and judge-output shape should be confirmed by scout before builders.

B/C: hooks/multi-inbox.js, hooks/multi-inbox.test.mjs, scripts/mirror-shared-skills.mjs and its existing test file only. Positive child agent_id returns immediately after input parse before registry/cursor/stamp/binding effects. Lead must still receive pending note. Do not guess Codex discriminator. Add work-record.md to existing shared docs mirror inventory and appropriate regression. Add bearings to PLUGIN_SKILLS and source inventory comment so D can actually reach Codex through the existing mirror; integrate with D before claiming discovery. This lane can follow first completed/reviewed territory to respect three available worker slots, or replace a blocked lane. No install or live hooks edits.

## Build mechanics

One scout surveys D,A,G,B/C before territory worktrees. Output scout-D.md, scout-A.md, scout-G.md, scout-BC.md in this directory, <=40 lines each. Check claimed base premises and tests. Source read-only at main checkout C:/Users/benzh/Code/claude-delegation.

No new shared executable contract: these independent patches extend existing modules and D is prose. Existing committed interfaces at base are contract stubs; inventing new interface files would add no value. A and G must not share a new helper just to unify similar symptoms. Independent reviewers challenge tests and evidence, with exact artifact SHA in first line. Parent owns docs/work records, builders never edit those.

Builders mid tier Terra, independent reviewers Astra. No nested agents. Scoped tests through scripts/run-tests.mjs using its existing filter interface. One expensive verification at a time: request parent slot before running a test suite, do not hold an idle OS mutex. Full suite only on integrated candidate. No npm install, servers or browsers. Worktree commits use existing identity. Reports in this directory named D-report.md etc., state files named D-state.md etc. Reports start VERDICT: PASS/FAIL/PARTIAL/BLOCKED and record commit, changed files, exact checks, limitations, Cause / Discriminating check / Fix location / Simplification for fixes. Parent adds independent regression checks during review as needed; builder tests are not sole acceptance gate.

Hypothesis: callable goal assessment plus removal of result-loss bugs produces usable work and honest next-work selection without a new coordination engine. Measure each lane ask-to-reviewed time, attempts/rework, retained planned results, and baseline findings. Token counts and seven-day outcomes unavailable until actual evidence exists; do not fabricate.

## Scout adjudications

D publishes through the existing notion-writing skill. No new helper. Canonical builder page is configured per project, never hard-coded to Ben. Callable skills can be read from their repo path before installed discovery is verified; report that distinction.
A saves priorBuildSha in the fix-round local scope before reassigning build, passes it as an explicit argument to reviewPrompt for both first attempt and retry. Initial review must match build.sha too. Only review.verdict APPROVE and review.sha matching build.sha allows inclusion in approved; INTEGRATE schema remains unchanged. Null parallel slot becomes a blocked result with territory identity and reason. Preserve exact top-level shape.
G adds coverage as a fourth top-level key, updating old shape test and delegate documentation intentionally. Read/research agent schema: {status: 'complete'|'unavailable'|'unverified', finding: string, sources: string[], reason: string}. Schema-backed result complete requires nonempty finding and at least one nonempty source reference; otherwise unverified. Null becomes unavailable, absent slot not-run. Legacy free text is unverified. This is evidence attribution, not proof the model accessed the source, explicitly tell judge to check material references. Failed reads never remove research slots.
BC positive child means nonempty string input.agent_id; every event returns before side effects. Existing hooks/reminder.js is source evidence for the field, not a claim about all providers. Assert both source inventory and copied destination in synthetic fixture with D files present. No real installed writes.
