# Codex parity territory and review plan

Base: 8cebedaf39f9efee5d324d00c22b44e18d25fe7a. Delivery branch: build/codex-parity-1. Scope: ../2026-09-25-codex-goal-and-bearings-parity.md. No main merge, installation, new event, scheduler, Notion edit, identity changes or release bump.

- P1 owns hooks/multi-codex-hook.mjs and its test; may extract shared card/notice adapters into hooks/lib/goal-context.mjs and update hooks/delegation-reminder.js plus its tests only for reuse/parity. Reuse scripts/goal-card.mjs and bearings-state.mjs; no second renderer or receipt store. Goal/card source wording is not edited.
- P2 owns scripts/build-census.mjs, its tests/fixtures, docs/census.md. Actual current Codex session metadata/usage only, never private content or historical read-only session. Explicit unsupported is acceptable where evidence cannot support complete attribution. Preserve Claude output/API unless additive host reporting needs an explained change. Do not fix the two census-base findings here: coordinate upstream separately, preserve known limitations.
- P3 owns skills/bearings/SKILL.md, docs/native-use.md and README changelog only. May read other territories; no duplicate source edits. Distinguish packaged behavior, observed live behavior and installed versions.
- Lead exclusively owns specs, records/evidence, review requests and integration. Builders commit only owned source paths in separate Orca worktrees cut from one pinned prepared base. Mid-tier Codex writes; skills-o high-tier Claude reviews exact SHA. Integrator runs one sealed full gate after territorial focused gates and review.

Interfaces: existing goal-card exports and bearings check receipt remain authoritative. P1 owns any new shared-helper API and records it in its scout/brief before coding; only its two host adapters consume it. P2 has no dependency on P1 runtime; P3 describes those reviewed contracts. No artificial stubs are needed between independent runtime territories; commit this boundary and any agreed helper signature before dispatch.

Review pacing: targeted reviews requested as soon as each build passes focused gates, not after a whole-wave barrier. One check at 5 minutes after a peer request without ACK, then use the allowed independent Codex high-tier fallback for that territory if peer unavailable. Never wait inside a turn solely for a peer. Builder ETA 12 minutes P1, 10 minutes P2, 5 minutes P3; one progress checkpoint then adjust based on evidence. Full gate serialized, small focused node fixture tests allowed independently in scratch homes.

Acceptance: exact artifact review, shared record check-acceptance then accept; use --no-census with a precise reason if Codex attribution is unsupported. No fabricated cost, turn count, spec timestamp or completion claim. Starting observation is the actual task-admission time; the spec supplies only a date, not a trustworthy precise ask time. Preserve the open census-review findings rather than silently treating a passing baseline as correctness.
