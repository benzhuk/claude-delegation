# Reproducible native continuation regression

Outcome: preserve the real failure that appeared after two native tools as a repeatable developer gate, rather than relying on temporary session files or hand-fed hook callbacks. This is a prerequisite-met parallel test territory: the actual native Claude fixture and failure already exist; the bounded reader repair is independent.

Own only `scripts/native-continuation-smoke.mjs` and `docs/native-continuation-testing.md`. Reuse and simplify the independently corrected temporary `review-adapter-native.mjs` and `review-adapter-interrupt-replacement.mjs` in `C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923`. No new production runtime, installer, scheduler, provider abstraction or configuration format. Do not make the regular sealed suite require a native CLI or account.

Explicit opt-in CLI: `node scripts/native-continuation-smoke.mjs --claude <absolute-executable> [--output <new-output-directory>]`. It uses the current checkout's actual continuation CLI/hook. Test default-system-prompt bind activation and one correction, accounted silence, native SDK interruption and replacement prompt. Synthetic local Messages SSE serves only fixture responses; actual Claude emits hook callbacks and executes explicitly bounded fixture Bash commands. No inherited auth/provider/Orca/peer environment; OS/path/temp allowlist plus disposable homes, dummy key and loopback URL only. No real settings sources. No sandbox bypass flags. No copying authentication. Never inspect prior user transcripts.

Bound every child and server lifetime; preserve failed fixtures, report only owned paths, and terminate only owned processes. No recursive deletion of supplied paths. Reject an existing nonempty output directory. Success requires explicit assertions, not exit code alone. Report native executable/version, observed hook/request counts, bound identity/accounting state, cancellation acknowledgement and replacement disarming; mark unsupported/unobserved cases explicitly. Publish sanitized structural summaries, not full prompt bodies or environment. Return nonzero on failed assertions.

Keep the implementation compact by using one local fixture server/process driver and per-scenario state. Preserve valid streaming frames, asynchronous subprocesses, true native interrupt protocol, and default-system-prompt coverage. Source review and one actual native run are required before integration. This runner proves mechanics, not model judgment, cross-provider useful output, total token cost or universal host support. Codex proof remains a separately qualified route while command launch is diagnosed; do not silently replace the production adapter with an MCP transport.
