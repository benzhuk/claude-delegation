# Respect the canonical decisions archive

Live0.20.5 qualification failed safely: the actual canonical page has one OPEN decision, Done false, no reader warnings, and eight optionless historical summaries below its top-level Closed heading. parseDocument reports those archived summaries as shapeless; pickup therefore reports reconciliation and never becomes usable. Source tests exercised synthetic pages without the real archive structure. The private live page is evidence, not a fixture to publish. The single Windows registration is paused; no note or receipt was created.

Goal: preserve the existing page and all human signals while making its shared reader distinguish historical optionless summaries from malformed active decision toggles. Fix the shared classification, not the Notion history or just the unchecked-Done guard. Existing attended hand-back and pickup callers should receive the same corrected interpretation.

Pinned API: parseDocument(text,{now}) and its result keys remain unchanged. No new export/interface is required; the existing committed API at7198fd3 is the contract. No new parser package, Markdown engine, state file, schedule or page rewrite. All existing Done, warning, malformed-summary/fence, default, checked-option and comment rules remain in force.

Archive scope is established only by an unindented level-one heading whose visible canonical title is exactly Closed, including its existing supported Notion toggle attribute form. It lasts until the next unindented level-one heading outside a fenced block. Do not infer archive state from arbitrary summary titles, prose, indented/nested headings or names merely containing Closed. Inside that explicit scope, optionless summaries are historical and omitted only from shapeless. Their human comments must STILL reach unattached, their checkbox options must STILL become decisions with existing warnings/statuses, and malformed syntax must STILL be blind. Do not filter whole archive subtrees or suppress user signals. Outside that scope the existing shapeless check stays unchanged. No internal scope marker may leak into public result shapes.

Source territory: decisions-read.mjs, decisions-read.test.mjs, decisions/SKILL.md (small clarification only). Independent contract territory: a new decisions-archive.contract.test.mjs with a synthetic privacy-safe canonical page, archived nested summaries, active malformed toggle, comments under Closed, checkboxes under Closed, scope exit and fake/fenced/indented Closed headings. Include a real pickupOnce check of unchecked page and checked page with one real current selection plus archive: no archive refusal, correct capture/send count, comments retained. No actual Notion content or user decisions in fixtures. Root owns spec, work record, release and live qualification.

Scout before isolated worktrees, independent high-tier spec check, then parallel source and contract work. Required discriminator: reproduce the source0.20.5 false rejection using synthetic archive plus a read-only parse of the private real page; after fix, archived optionless summaries no longer block, active defects still block, all user signals survive. Independent review, focused tests and parent full sealed gate precede0.20.6. Re-activate only this same registration after installed verification; do not manufacture a checked human submission.

Scout and independent spec review approved this repair. Parent clarifications: do not use decision-title suffix normalization to turn `# Closed (8)` into canonical Closed. A column-zero heading inside a details block is still nested and must not enter or end top-level archive scope. Track only the minimal structural depth needed to recognize that boundary, outside fences; do not add a Markdown parser. Add this independent discriminator and test direct public parse result keys for internal scope marker leakage. Pickup's invalid-before-Done guard remains intact.

## Live discriminator correction and architectural ruling

The original all-eight-within-Closed premise above was wrong. Read-only qualification of a05cf86 against the preserved private page reports five summaries correctly archived and three still shapeless below a later, separate historical H1. The source boundary is behaving as specified. Preserve the original failure and this correction; do not widen archive inference to every section other than Waiting.

Root positively inspected that later historical research section and will make a bounded, fresh, anchored hierarchy-only Notion edit placing it within Closed. Preserve its title, all prose and code, ordering, comments, options and Done state. No deletion, arbitrary historical-title allowlist, page replacement, or fabricated submission. The existing authorization to maintain these project documents covers this structural correction. The earlier hand-back build's keep-as-is restriction was a scope choice for that build; history remains intact.

Eliminate the competing hand-back interpretation: source territory now additionally includes decisions-handback.mjs and decisions-handback.test.mjs. Replace its permissive backward heading scan with direct reporting of shared doc.shapeless, retaining the outward SHAPE text and existing other checks. Historical summaries inside explicit Closed remain valid; unknown later active sections with optionless summaries block both callers. This removes a duplicate classifier instead of adding another exception. Independent test territory may add a synthetic mixed/normalized pair and direct hand-back CLI discriminator using existing sealed fixtures; no live side effects.

The private page must still show three shapeless summaries before the hierarchy edit and zero afterward. Verify exact intended structural delta and preserved human signals after fresh readback, then installed qualification. The edit is not itself source acceptance or automatic delivery evidence. Source review and full tests still precede release0.20.6.
