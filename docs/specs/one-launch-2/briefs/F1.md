Task: Fix four defects in the build-loop Workflow script and add a new deterministic
accept-prep helper it calls as one command: (1) accept-prep must write the record's
reviewed `Log:` line BEFORE running the census, never after; (2) accept-prep must never
replace the record's whole header — it changes only the fields it owns and preserves
every other line and byte; (3) given-territory mode must accept a `seamBriefPath`
argument, falling back to `reviewerBriefPath` only when absent; (4) `baseSha` must be
exactly one git sha (7-40 hex) — anything else (e.g. `"a+b"`) returns at once with
`blockers: [{id:'*', reason:'bad-base-sha'}]` and spawns nothing. Also add one SKILL.md
sentence: when a build needs two bases, the lead makes the merge commit on the
integration branch before launch and passes its sha; the record carries
`Base: <merge sha>` and `Base-of: <sha>, <sha>` for humans.

Goal: lane seven (one-launch fix round) — lane six's first real setup-mode use of this
script exposed these four defects; this build fixes them so the next setup-mode build
doesn't lose header lines, doesn't refuse accept on census-stale, can brief a seam
review in given-territory mode, and never accepts a two-sha Base.

Work: wr-2026-09-26-one-launch-fix (docs/work/wr-2026-09-26-one-launch-fix.record.md)

Inputs (by path):
- /home/ben/Code/wt-olfix/docs/specs/one-launch-2/spec.md (Why section — lane six's four
  defects verbatim; Territory section — the four fixes and Acceptance)
- /home/ben/Code/wt-olfix/docs/specs/one-launch-2/contracts.md (R1-R6 — the lead's
  rulings win over spec.md where they differ; R2 pins the accept-prep.mjs CLI shape and
  step order exactly; R3 pins its test list; R4 pins the loop-script changes; R5 pins the
  one SKILL.md sentence; R6 pins your gate)
- /home/ben/Code/wt-olfix/docs/specs/one-launch-2/briefs/scout-F1.md (this territory's
  scout addendum — read it before you start; where it disagrees with the spec, the spec
  wins, but its open questions are yours to resolve and record how)

PROJECT FACTS (repo root /home/ben/Code/wt-one-launch-2-F1, package manager: none — plain
Node ESM, no build step. Test command for THIS territory:
`node --test skills/team-build/references/build-loop-workflow.test.mjs
skills/team-build/references/accept-prep.test.mjs`. Full suite is
`node scripts/run-tests.mjs`, run only by the integrator, not you. Git identity is never
set by you — the sealed test runner already gives tests their own throwaway identity;
never touch the real machine's `git config`. Never push, never touch `docs/work/` (the
orchestrator's only writer), never send a peer note.):

NOT (out of scope, stated explicitly):
- `scripts/`, `hooks/`, `.codex-plugin/` — nothing under these paths, per contracts.
- Any README changelog edit — the RESULT carries the one-line entry, not you.
- SKILL.md edits beyond the one sentence R5 pins — no restructuring, no other wording
  changes.
- `docs/work/` — you never write there; the lead owns the work record.
- Dogfood (rewriting lane six's record's Base to its merge commit and running four-read
  on it) — R5 names this explicitly as the LEAD's accept-turn job, not yours.

Evidence format: cite file:line for every claim in your report; for the ORDER test (R3b),
paste the actual stub invocation log showing the census stub saw the record's `Log:`
line already present, AND demonstrate the negative (a patched variant that calls census
first fails that same test) per R3's explicit instruction — quote both runs.

Report: /home/ben/Code/wt-olfix/docs/specs/one-launch-2/reports/F1-report.md. Line 1 is
the verdict, first word.

Gate: node --test skills/team-build/references/build-loop-workflow.test.mjs skills/team-build/references/accept-prep.test.mjs > /home/ben/Code/wt-olfix/docs/specs/one-launch-2/reports/F1-gate.log 2>&1. Read only the tail and the failing names. No wrapper script.

State file: /home/ben/Code/wt-olfix/docs/specs/one-launch-2/reports/F1-state.md. Keep it
current after every gate.

A result of zero, "not found" or "could not determine" is a good answer. Say what you
tried. Do not guess.

Autonomy: you decide accept-prep.mjs's internal module layout, exact option-parsing
code, and whether to import `formatLogLine`/`parseRecord` from `scripts/work-record.mjs`
directly or shell out to it — R2 pins the CLI's flags, step order, and output shape, not
its internals. Check in before changing any flag name, output field, or step order R2
pins, or before touching a file outside this territory's list.

Un-agent-able steps: none — this territory is entirely mechanical (a script fix, a new
CLI helper, tests, one doc sentence).

ETA: 60-90 minutes (loop-script fix plus one new tested helper). Report or park by then.

Termination: report to the path above, first line `VERDICT: <word>`, then stop. A bare
"Done" means read the file; nothing is trusted from a final message alone.
