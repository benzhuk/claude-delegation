Task: Add one rule to team-build's acceptance step (skills/team-build/SKILL.md) that, after
`accept --census` and the push, the lane lead posts its own merge item to the owner's
decisions page (through the existing decisions-skill anchored-edit route and the
decision-item template) and only then sends its RESULT; add one paragraph to
skills/decisions/SKILL.md naming the shape of a lane-posted merge item; and, in your report
(not code), state plainly whether a Codex mirror of the decisions skill can post the item
today.

Goal: lane six (collect-from-origin) — an accepted branch becomes visible to Ben on the
decisions page itself, not only through a note that may never reach the lead.

Work: wr-2026-09-26-collect-from-origin (docs/work/wr-2026-09-26-collect-from-origin.record.md)

Inputs (by path):
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/spec.md (Territory C2, items
  1-4; Acceptance)
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/contracts.md (R3 — wins over
  spec.md where they differ; note the one-launch SKILL.md note)
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/briefs/scout-C2.md (this
  territory's scout addendum — read it before you start; it names the exact anchor
  sentence in SKILL.md's Ship section and the existing test that constrains your edit)

PROJECT FACTS (repo root /home/ben/Code/wt-collect-from-origin-1-C2, package manager: none —
plain Node ESM. Gate command for this territory:
`node --test skills/decisions/scripts/skill-text.test.mjs` (the test currently proven to
read one of your three files — scout-C2.md explains why the other two have none today).
Never set a git identity, never push, never touch `docs/work/`, never send a peer note.):

NOT (out of scope, stated explicitly):
- Territory C1's files: `scripts/collect-from-origin.mjs`, its test, its fixtures,
  `docs/census.md`.
- `scripts/build-census.mjs`, `scripts/work-record.mjs`, `scripts/four-read.mjs`,
  `scripts/janitor.mjs`, `hooks/`, `.codex-plugin/`, `README.md` — off-limits per contracts.
- No README.md changelog edit (spec item 4) — your RESULT carries the one-line entry
  instead, in your own report.
- Do not invent a new posting command — the route is the existing `notion.js edit`
  anchored-edit flow already described in skills/decisions/SKILL.md; name it, don't
  build a new one.
- Do not remove or reword any exact literal string `skill-text.test.mjs` asserts on
  (scout-C2.md lists them) — your gate must stay green.

Evidence format: cite file:line for every claim; quote the exact before/after diff of
each of your three edits in your report; answer scout-C2.md's two open questions.

Report: /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C2-report.md.
Line 1 is the verdict, first word.

Gate: node --test skills/decisions/scripts/skill-text.test.mjs > /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C2-gate.log 2>&1. Read only the tail and the failing names. No wrapper script.

State file: /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C2-state.md.
Keep it current after every gate.

A result of zero, "not found" or "could not determine" is a good answer. Say what you
tried. Do not guess.

Autonomy: you decide exact wording/placement within the pinned anchor points (R3); check
in before restructuring either SKILL.md's section layout or the decision-item template's
existing fenced shape — that needs a spec ruling.

Un-agent-able steps: none — this territory is entirely doc prose.

ETA: 30-60 minutes (pure doc-edit territory). Report or park by then.

Termination: report to the path above, first line `VERDICT: <word>`, then stop. A bare
"Done" means read the file; nothing is trusted from a final message alone.
