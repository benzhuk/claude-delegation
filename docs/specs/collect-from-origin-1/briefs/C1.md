Task: Build `scripts/collect-from-origin.mjs` (plus its test and any fixtures) — a
read-only CLI that lists every non-main `origin/*` branch, finds any `docs/work/*.record.md`
that differs from main on that branch, and prints one row per changed record with its
merge/accept state. Add the three required sentences to `docs/census.md`.

Goal: lane six (collect-from-origin) — a durable, read-only signal for accepted-but-unmerged
work, so a lead never again reports a pushed, accepted branch as merely "in flight."

Work: wr-2026-09-26-collect-from-origin (docs/work/wr-2026-09-26-collect-from-origin.record.md)

Inputs (by path):
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/spec.md (Territory C1, items 1-3;
  Acceptance)
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/contracts.md (R1, R2, Gates — wins
  over spec.md where they differ)
- /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/briefs/scout-C1.md (this
  territory's scout addendum — read it before you start; where it disagrees with the spec,
  the spec wins, but its open questions are yours to resolve and record how)

PROJECT FACTS (repo root /home/ben/Code/wt-collect-from-origin-1-C1, package manager: none —
plain Node ESM, no build step. Test command for THIS territory:
`node --test scripts/collect-from-origin.test.mjs`. Full suite is
`node scripts/run-tests.mjs`, run only by the integrator, not you. Git identity is never set
by you — the sealed test runner (`scripts/test-home.mjs`) already gives every test its own
throwaway identity; never touch the real machine's `git config`. Never push, never touch
`docs/work/` (the orchestrator's only), never send a peer note (no note-send/multi skill).):

NOT (out of scope, stated explicitly):
- Territory C2's files: `skills/team-build/SKILL.md`, `skills/decisions/SKILL.md`,
  `skills/decisions/templates/decision-item.md`.
- `scripts/build-census.mjs`, `scripts/work-record.mjs`, `scripts/four-read.mjs`,
  `scripts/janitor.mjs`, `hooks/`, `.codex-plugin/`, `README.md` — off-limits per contracts.
- Any `git` operation beyond the read-only plumbing named in contracts R1 (`for-each-ref`,
  `show`, `cat-file`, `merge-base --is-ancestor`, `log -1`, `diff --name-only`/`ls-tree`) —
  no checkout, no write, no temp files, no `git fetch` except the one the CLI itself is
  specified to run (or skip with `--no-fetch`).
- Pushing anything, setting a git identity, sending a peer note.

Evidence format: cite file:line for every claim in your report; quote the CLI's actual
output for at least one fixture run; state your answers to scout-C1.md's two open
questions and why.

Report: /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C1-report.md.
Line 1 is the verdict, first word.

Gate: node --test scripts/collect-from-origin.test.mjs > /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C1-gate.log 2>&1. Read only the tail and the failing names. No wrapper script.

State file: /home/ben/Code/wt-collect-1/docs/specs/collect-from-origin-1/reports/C1-state.md.
Keep it current after every gate.

A result of zero, "not found" or "could not determine" is a good answer. Say what you
tried. Do not guess.

Autonomy: you decide implementation details (exact function names, module layout, which
of `diff --name-only`/`ls-tree` to use) within the pinned CLI contract (R1). Check in
before changing the CLI's flags, output fields, or exit-code behavior from what R1 pins —
that needs a spec ruling, not a builder judgment call.

Un-agent-able steps: none — this territory is entirely mechanical (a CLI, a test, docs).

ETA: 60-90 minutes (pure-code territory with a test harness). Report or park by then.

Termination: report to the path above, first line `VERDICT: <word>`, then stop. A bare
"Done" means read the file; nothing is trusted from a final message alone.
