# Harness ready queue — September 23, 2026

Owner: skills-a. Governing goal: turn intent into useful verified outcomes through aligned, autonomous, parallel agents at appropriate total cost. Ben approved merges directly; installation, live machine configuration and durable knowledge publication remain separately scoped decisions. Independent source work continues.

## Implemented and merged

0.14.0 at 9adb625 contains callable bearings and a published first assessment, artifact-aware build-loop reviews, preserved null territory results, the positive Claude child inbox guard, mirrored work-record reference, honest ladder coverage/budgeting, and attended human-submit Done handling with renderer plus targeted Notion edits. These must not be researched or rebuilt as outstanding defects. Source gates are not installed-host evidence.

## Current delivery and active source work

0.15.0 is merged and pushed at 77ce8be: strict source acceptance, portable decisions configuration, current provider-neutral goals/card and finite continuation. Full executable candidate 4037d passed 1,282/1,282 sealed checks and independent seam review. Final release commit changes records/evidence only. Installed behavior is not established.

| Territory | Current state | Next |
|---|---|---|
| A/B/C | Source accepted in 0.15.0 | Useful host pilots and outcome measurements |
| D | Daily bearings notices and explicit completion receipts independently approved and integrated | Source accepted in 0.16.0; full corrected candidate passed 1,290 checks |
| E | Source accepted in 0.17.0 after independent integrity and package reviews; full gate 1,312/1,312 | Useful native trials, reader deployment and separately scoped activation; full unattended owner recovery remains |
| F | Audience routing independently approved and integrated | Source accepted in 0.16.0; no extra template system |

Three worker slots plus the lead. Reviews begin as territories deliver; useful ready work refills capacity. Full sealed suite has one owner while other lanes edit/review. The source release does not wait for unrelated rejected E work. Source accepted and installed/live validated remain separate states.

## Active follow-through after the premature stop

0.17.1 is merged at 49c1b64: continue now applies at ongoing wave/release/review/status closeout, with short team-build and delegate caller routes. Three skill validators, 11 mirror-inventory checks and independent seven-scenario review passed; strict acceptance checked the deliberately pinned bcbf466 source artifact. On September 23 at 7:39 PM America/New_York, the current Windows mirror was updated from 0.13.0 to released 0.18.0 under Ben's direct skill-update instruction. All eight skill files match source and fresh Codex prompt-input discovers continue. Config and hook bytes are unchanged. This is local discovery evidence, not automatic behavioral compliance or other-machine rollout.

- Native Codex package: 0.18.0 is merged and pushed at 686e778 after independent review and 1,315/1,315 sealed checks on executable candidate dddb546. Actual local CLI discovery found all nine namespaced skills and zero plugin hooks. One portable manifest plus an explicit empty OpenAI hook declaration prevents accidental Claude-hook discovery; existing Claude hook bytes are unchanged. No persistent installation or trust changes.
- Native Claude: Opus independently approved the bcbf466 three-skill continuation patch in 66,092 ms, with zero permission denials. This is an attributable Read/stdout review, not native packaging review or file-writing proof. Two Sonnet guide attempts delivered no guide. A separate one-file probe attempted no Write; a harmless argv fixture subsequently reproduced a launcher defect that truncated that probe's task. Diagnose and correct the transport before any further permission conclusion.
- Native coexistence: disposable probes distinguished local `multi` from `delegation:multi`. Windows still read the real personal skill root despite redirected home variables. Discovery proves namespace/source distinction, not live precedence, duplicate-free behavior, or complete profile read isolation.
- Non-code records: 0.18 includes the independently reviewed documentation correction at 17759c3. Stable report/URI references and actual evidence are permitted without invented Git commits; strict code acceptance is unchanged. A Codex-authored operator guide was independently reviewed after correcting one overstated Claude-review attribution.

Current finite follow-through: guide and scout records are integrated at 9270303. The temporary launcher now has one actual argument source, independently verified through real child argv; a fresh native Write made exactly the one expected file with exact bytes and zero denials. Earlier fixed-1 was an invalid Read-only test and is corrected explicitly. No more setup-only Write trials are selected. The bounded Codex investigation found temporary-home device authentication as the supported next route under current constraints. A fresh independent bearings assessment selected RE-PLAN. Native Claude delivered a proposed acceptance table; Codex rejected its material factual errors. A narrow Codex repair was independently approved and is the current table below. The rejected original and actual scoped readings are preserved. The one-pass prediction failed; no additional native author or setup retry is selected. Publish the assessment and lead response, then exercise the existing host routes when their specific dependencies are available. No new packaged mechanism.

The scoped cadence/recovery scout found no justified new scheduler or blind automatic owner replacement. That conclusion applies only to those mechanisms, not to the whole goal. The native package and skill correction are concrete useful independent work and proceed.

## Next available capacity

| Remaining baseline claim | Current evidence | Exact missing observation for acceptance | Smallest next action | Actual dependency / owner |
| --- | --- | --- | --- | --- |
| Native Codex package execution | 0.18 source is released; disposable CLI installation discovered nine namespaced skills and zero native hooks. Actual native model use, personal-skill precedence and persistent installation are unproved. | A fresh model session uses the released namespaced skill on a useful task and produces attributable reviewed output; a catalog listing alone does not pass. | Use the prepared temporary-home route after device sign-in, or the separately authorized persistent rollout route. | Attended sign-in for the inspected temporary route, or Ben's existing rollout choice. No credential copying or automatic dual install. |
| Existing Codex mirror on chosen hosts | Current Windows mirror is updated to 0.18.0: eight skill files match source and fresh discovery finds continue; roles/shims and config/hooks were unchanged. Other hosts remain unverified. | A fresh session uses the released skill on useful work; separately verify selected roles/shims and hook behavior. | Use the updated local skills on the next useful task; extend rollout only under its existing broader choice. | Local skill update is complete under Ben's direct instruction. Other-machine rollout and hook activation retain their separate scope. |
| Claude installed-host behavior | Inline plugin discovery and a native useful Read/stdout review were demonstrated; a separate narrow Write probe passed. Persistent 0.18 installation and installed hook behavior are unproved. | A fresh installed-host session discovers the released plugin and completes an authorized useful task with attributable result evidence. | Use an existing ready task after scoped rollout; do not repeat the Write setup probe. | Existing rollout choice; the authorized agent performs setup and chooses work within the established goal. |
| Mixed-provider useful outcomes | Codex-authored bcbf466 was accepted and independently reviewed by native Claude Opus. Claude delivered a checklist in 83.339 seconds, but Codex rejected it and repaired the queue. These are real cross-provider events, not automated peer delivery. | An accepted Claude-authored useful artifact establishes that still-missing direction; actual automatic peer delivery needs its own observed transport/owner result. | Use the next real project deliverable for the missing direction; do not recreate the guide or checklist solely to obtain a positive trial. | Available useful task and host route; no blanket new authorization. Use multi for actual equal peers; neither provider must spawn the other. |
| Bearings cadence and completion | D source provides Claude event notices and explicit completion receipts. It has no automatic daily assessment, release watcher, or automatic KILL completion. | An installed host surfaces a due notice and an explicitly invoked assessment records its result; a changed release/KILL condition is handled by another explicit invocation. | Design the attended host observation around a real project assessment. | Chosen installed host and task owner; no scheduler or unattended completion path is available. |
| Done pickup and orphaned-owner recovery | E source has one-shot reader, recorded-result/recovery rules and conservative delivery; repeated --id is not idempotent. Known-owner pickup and automatic replacement are separate claims. | A real submitted round reaches its bound known owner once and is accounted without losing newer edits; uncertainty causes no duplicate consequential action. Separately prove a replacement's liveness and authority before claiming recovery. | Exercise the existing one-shot reader on an actual submitted round after scoped activation; keep orphan recovery explicitly pending. | Reader activation and a human-submitted round. Reliable liveness/rebinding evidence is an additional dependency only for automatic owner replacement. |
| Codex child inbox isolation | 0.18.1 uses matching native session metadata to suppress positive child inbox operations; actual parent/child metadata and representative-size fixtures passed. Unknown metadata retains delivery. | An actual child hook carries its own matching session/transcript identity and leaves the lead cursor untouched; the intended lead handles a real item. | Observe that existing host route during useful work; do not infer historical note attribution or universal isolation from source tests. | Current event evidence and a real actionable handoff; no guessed Claude-field parity. |
| Durable memory and upkeep | A concrete durability experiment is prepared using existing capture and chezmoi paths. Publication, triage of private backlog, and automatic upkeep are not established. | A permitted publication/retrieval exercise shows a captured lesson survives the intended path and can be retrieved; upkeep then has explicit enablement, non-overlap, off switch, and visible digest. | Keep the prepared experiment available; do not run publication or triage from this queue. | Ben chooses publication/writer authority; no new memory engine or schedule. |
| Audience and document usefulness | F source routing is accepted; live audience usefulness is unmeasured. The Codex-authored native-use guide is already accepted and published, so it is not pending work. | A real builder/owner document is routed through the existing canonical goals/decisions conventions and its intended reader can use it. | Include audience evidence in the selected useful task rather than create another template system. | Selected task and its owner/readers. |
| Outcome measures | Current measures for whole-task cost, comparative ask-to-accepted time, seven-day rework, and stalled work remain unknown or incomplete. | The selected useful pilot records its complete task boundary and later comparison/rework evidence without treating source-suite timing as product improvement. | Attach a small evidence receipt to the chosen host/mixed-provider task. | A completed useful task and later observation window; no new acceptance engine. |

## Integration and measures

Delivery reliability follow-through: 0.18.1 is merged and pushed at 6328628. Its executable candidate 3f4d213 passed 1,322 sealed tests and independent Astra review. The current Windows mirror is updated and existing config/hook registration hashes are unchanged. It fixes the Windows executable/argv boundary, positive Codex child isolation, and pickup off-switch handling. See `2026-09-23-continuation-delivery-contract.md`; a seen cursor still does not establish owner disposition, and the session-to-authorized-work binding needed for bounded runtime continuation remains open.

Current independent ready work: private decisions captures outside Git with sanitized peer pointers and zero-item fresh admission (`wr-2026-09-23-private-decisions-capture`); automatic Details linkage when note-send writes a packet. These are concrete defects observed in current source/use. They do not activate unattended pickup, migrate legacy state, add model polling, or replace the existing work ledger.

Keep the four outcome measures. Current source-suite counts and review timing are observations, not measured product improvement. Record unknown token totals, pending seven-day rework and untested host behavior. Learned causes and simpler designs should change the queue. A human decision blocks its dependent actions, not this queue. Stop only when the agreed objective is achieved or all remaining useful authorized work has actual external blockers.
