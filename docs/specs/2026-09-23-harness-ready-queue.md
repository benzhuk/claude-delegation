# Harness ready queue — September 23, 2026

Owner: skills-a. Current objective: turn authorized intent into useful verified outcomes through aligned, autonomous, parallel agents at appropriate total cost. [Goals](../GOALS.md) govern; this queue records current delivery and dependencies. Earlier release chronology is retained in Git and dated work/evidence records, rather than repeated as current work here.

## Current delivery

Released source and the current Windows shared-skill mirror are 0.20.1 at `7f2f0c9`. Final sealed source gate: 1,387/1,387; prior 0.20 native integration gate: 1,378/1,378. The local mirror's eight skill files match source; personal config and hook-registration hashes did not change. Broader installation, native hook trust/refresh, other-machine rollout and durable knowledge publication remain separate unchecked owner choices.

The source now connects one shared continuation core to both native hosts. Explicit ongoing authority, native session/epoch, existing selected work records and exact evidence accounting govern one correction opportunity at the existing Stop boundary. It adds no idle polling, wake scheduler or second work ledger. A prompt and a bounded correction are not a guarantee of sustained model diligence.

Actual native Claude SDK/print and installed Codex App Server runs exercise bind, account, one correction and native interruption; Claude additionally rejects stale binding after prompt replacement. These used local synthetic provider responses. Installed Codex packaging exposed nine skills and five hooks through the supported compatibility manifest. Child callbacks corrected the previously modeled identity contract. See [native Codex proof](../work/evidence/codex-native-core-qualification.md) and [final source seams](../work/evidence/continuation-final-source-approved-811e548.md).

The installed Codex SDK selects Store PowerShell for its outer hook runner and fails before invoking the handler; changing only the disposable PATH to system PowerShell works. The upstream source chain is diagnosed, the installed SDK and production PATH remain unchanged, and an accepted useful Claude-authored issue draft remains unpublished. See [cause evidence](../work/evidence/codex-command-hook-diagnosis.md) and [draft](../work/evidence/codex-sdk-hook-shell-issue-draft.md).

## Finite work in flight

- **Diagnostic release 0.20.1 complete:** artifact `c50fb22` has independent Astra approval, 47 focused tests and 81 adverse/compatibility checks. Final full integration passed 1,387/1,387 on `cea599a`; `7f2f0c9` is merged/pushed and the authorized local mirror is verified. Intermediate `ae613fc` passed its review and 1,382 full tests, but a late native review exposed missing check-definition admission; release was held and acceptance reopened. The repair uses one shared definition/read boundary. [Work record](../work/wr-2026-09-23-wiring-unknown.record.md).
- **Instruction consistency:** candidate `aa42a11` repairs dependency admission, reviewer tiers, environment ownership, ETA handling and report evidence in three existing instruction files. Builder validation passed; an actual native high-tier reviewer now evaluates the changed artifact and eight scenarios. The earlier Sonnet attempt produced no artifact: its brief incorrectly limited native binding context to UserPromptSubmit and its180-second cap killed ongoing work. That prediction stays failed. The new source review is required useful work, not a rerun to improve the old score.
- **Independent bearings completed:** fresh Astra chose CONTINUE, the four-question assessment and lead response are published in Notion, and the packaged matching-goal completion receipt was written at11:22PM America/New_York. Its one-run live prediction failed and is preserved. The new instruction correction and actual review follow observed gaps; no fixture-only campaign is selected.

The three worker slots are capacity, not a target requiring busywork. Independent useful lanes start promptly. Full sealed integration has one owner with executable source frozen. Reviews start as artifacts arrive; main receives only accepted source, not a moving worker branch.

## Remaining outcomes and exact dependencies

| Outcome | Current evidence | Next useful observation / action | Dependency |
| --- | --- | --- | --- |
| Native Codex useful work | Real installed-package lifecycle works in disposable tests; no actual useful model task through that installation yet. | A fresh authorized native session uses the released skills to deliver attributable useful work. | Supported provider authentication for the disposable route, or chosen persistent rollout. No credential copying. |
| Codex named roles | Both personal and project TOMLs appeared in the actual native selector; one project role applied model, effort, identity and developer instructions. | Use shipped roles in a real project; preserve sandbox override limits. | No new role framework needed; [qualification](../work/evidence/codex-custom-role-native-review.md). |
| Claude useful work with completion accounting | Native Opus found real source defects in one useful review, but the fixture lacked continuation context and denied Write; report bytes were recovered, not successfully written. | Review the actual instruction-fix candidate through the native high-tier route; account only observed evidence. The previous artifactless run is preserved as failed. | Current owned native task. [Prior partial result](../work/evidence/native-claude-useful-wiring-review-proof.md). |
| Mixed-provider outcomes | Native Claude reviewed Codex source; a Claude-authored SDK report was accepted with Codex corrections. | Use the next real project deliverable and actual transport when available. | Accepted report is not proof of automatic peer delivery or unedited first-pass acceptance. |
| Persistent installation / local hooks | Current local plain skill mirror is 0.20.1; source supports five hook events; existing four-event registration is unchanged. | Install/refresh only the chosen route, check trust/shell resolution and observe real useful host work. | Existing owner rollout decision; no automatic duplicate native+mirror hook routes. |
| Done pickup and owner recovery | One-shot reader, recorded-result handling and conservative recovery exist; known-owner pickup differs from automatic replacement. | A real submitted round reaches its bound known owner once and is accounted without losing newer edits. | Reader activation and actual human-submitted round; automatic replacement additionally needs liveness/authority evidence. |
| Bearings cadence | Callable assessments, Claude due notices and explicit completion receipts exist. | Observe an installed notice leading to a real assessment/receipt. | Chosen installed host; no automatic daily assessment or release watcher. |
| Durable memory | Capture/index/retrieval paths and a concrete existing-path durability experiment are prepared; new lessons are in the inbox. | Authorized publication/retrieval and later upkeep evidence. | Owner publication/writer choice; no unprompted triage, new memory engine or schedule. |
| Audience/document usefulness | Native-use guide and canonical Notion goal/decision routing exist. | The intended reader uses a real routed document; incorporate feedback into that document. | Reader feedback; no new template system. |
| Actual speed/quality/cost | Scoped native run time/cost and rejection history are recorded; whole-task tokens, comparative ask-to-accepted time and seven-day rework remain incomplete/unknown. | Attach complete available task boundaries to useful work and preserve later observation windows. | Real task/comparison and elapsed observation window; source-suite timing is not product improvement. |

Do not reopen completed historical defects merely because an older report describes them as unverified. Do not erase rejected trials or convert a passing suite into an installed/outcome claim. A source release, failed trial, empty inbox or pending human answer does not itself end ongoing work. Stop only when the authorized objective has completion evidence or every remaining useful authorized action has a concrete external dependency.
