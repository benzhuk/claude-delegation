# Third loop build: the four-number read

Written by skills-fable, 2026-09-25 late morning, for skills-o running the plugin's build loop. This is the next action of the independent bearings of 2026-09-25 (`docs/work/evidence/2026-09-25-bearings-assessment.md`, decision RE-PLAN scoped to the measurement lane). Its point is to stop patching the census and to make the goal's four measures a number a script prints for a build, or a stated `unavailable` with a reason. A second RE-PLAN on this lane stops it and goes to Ben's page.

Measure this build moves: all four become readable (top-tier tokens per build, hours ask to accepted, rework after acceptance, work lost or stalled). Must not worsen the others: lead under 20 turns, Sonnet builds, Opus reviews, no new mechanism (this reads what the census, the record, git and the ledger already hold).

Base: origin/build/integrate-0925 at 931588a (pending Ben's merge; do not wait for it). Branch `build/four-read-1`, one worktree per builder, never the main checkout.

## Rules that bind this build (from the bearings)
1. `scripts/build-census.mjs` is frozen. The one permitted change is a `--from <iso> --to <iso>` window that restricts which assistant messages of `--lead` are counted; nothing else in that file moves. Everything else lives in a new file `scripts/four-read.mjs`.
2. Net growth of `scripts/build-census.mjs` plus `scripts/work-record.mjs` is at most +150 lines over the base (base: 1036 + 1072 = 2108 lines). `scripts/four-read.mjs` is not in that budget but stays under 400 lines; a reader must be able to check the whole thing in one sitting.
3. skills-o leads this build from a FRESH session (a new session in the pane, not the one that led the census build). The record carries that session's id. No markers, no hand counts.
4. GOALS.md status lines that the evidence contradicts are corrected in this build, not later.

## Findings at 931588a (verified by a scout, file:line)
- `scripts/build-census.mjs`: `leadTurns` in `censusLeadFile` `:201-238`; usage fields summed in `addUsage` `:72-77`; `--marker` via `containsMarkerDeep` `:109,:220,:335`; JSON plus markdown output, deterministic (pinned by test).
- `scripts/work-record.mjs`: `FIELD_LABELS` `:43-48`; `Log:` regex `:106-115`; census lines copied verbatim `:117-125`; `checkAcceptance` `:723-925` with codes `acceptance-failed`, `census-missing`, `census-incomplete`, `census-stale`, `sha-not-in-git`; `acceptRecord` `:926-1036`; `extractCensusTimestamp` `:632-641` reads the first line only. Records name no session id today.
- `scripts/work-census.mjs`: `elapsedFor` `:82-98` measures Opened to LAST accepted; `countRounds` `:59-75`.
- Session id hint already exists: `hooks/lib/goal-context.mjs:39-42` `leadIdHint(sessionId)`, wired from `hooks/delegation-reminder.js:334-336,352,402` and `hooks/multi-codex-hook.mjs:84,147,154`.
- The hand-run baseline (`docs/work/wr-2026-09-21-next-build-t*`, Opened 2026-09-22T00:23:42Z, accepted about 01:56Z) names no session; its "152 turns" has no session source. Candidates are session 9c61c35a (persistent pane) and 7ce97c6a (from 2026-09-22T01:21Z); neither is proven. The read reports `unavailable (no lead session recorded)` for it unless a builder proves one of them from the transcript timestamps, in which case the proof goes in the evidence file.
- Git-derivable rework is weak today: loop-gates has one self re-accept commit; the census build's artifact is a merge commit.

## The four numbers, defined once (docs/census.md gets these sentences verbatim)
1. Top-tier tokens per build: the sum over every counted file of input, output, cache-read and cache-write tokens for messages whose `message.model` matches the top tier (`DELEGATION_TOP_TIER`, default `fable,opus`), plus the spec writer's slice: the spec session's top-tier usage between `Spec-from:` and `Opened:`. The census JSON already holds the by-model sums; the spec slice is one extra `build-census.mjs --lead <spec session> --from <Spec-from> --to <Opened>` run whose output file is stored next to the record.
2. Hours ask to accepted: `Opened:` to the FIRST `accepted` `Log:` entry, in hours to one decimal, plus the largest gap between two consecutive messages of the lead session inside that window (a stall indicator, printed beside it).
3. Rework after acceptance: the count of commits on `main` (or the integration branch when `main` does not yet contain the build) within 7 days after the first acceptance that touch any file changed in the build's range, excluding the merge commit and the release commit, plus the count of `accepted` `Log:` entries after the first. Both counts print with their shas or log lines. The range is `Base:` to the accepted sha from the record; a record without a resolvable range gives `unavailable (no range)`.
4. Work lost or stalled: the number of gaps over 30 minutes between consecutive messages of the lead session inside the build window, plus the number of ASK ids addressed to the lead's slug in `docs/ledger/*.md` within the window that have no RESULT or BLOCKED naming them with `re <id>`. Each gap prints its start time and length; each unanswered id prints.

5. Two companion lines, printed beside the four (measured on Netcup on 2026-09-25: a Fable lead's day was 273.6M cache-read tokens over 147 turns and 1,416 assistant messages, with 44 percent of the cache reads on turns that began with one of its own subagents finishing and 10 percent on peer-note wake-ups): top-tier assistant messages per build (each one re-reads the whole context, so this is the cost driver, not the turn count alone), with the tokens line split into cache-read, cache-write, input and output; and notes to the lead per build (ASK, RESULT and BLOCKED envelopes addressed to `Lead-session:`'s slug in the ledger within the window, since each one is a full lead turn). Rule for every lane from now on, stated in docs/census.md in one sentence (lane four repeats it in the team-build skill): a lane wakes its lead at most three times, ACK at start, RESULT at the end, BLOCKED if stuck, and an ACK's content is never sent under the ASK kind to force delivery.

Every number is printed with `value` or `unavailable (<reason>)`; a guess is never printed. Output is deterministic JSON plus a markdown table (pinned by a test), written to `docs/work/evidence/<record-name>.four-read.md` and `.json`.

## Territory R1: the read (scripts/four-read.mjs, its test and fixtures, scripts/build-census.mjs `--from/--to` only, docs/census.md)
1. `node scripts/four-read.mjs --record <record.md> --census <census.json> [--spec-census <json>] [--ledger docs/ledger] [--git <repo>]` prints the four numbers by the definitions above.
2. Inputs it needs from the record: `Opened:`, `Base:`, the accepted sha, `Lead-session:`, `Spec-session:`, `Spec-from:` (R2 adds the last three). Missing input never throws; it yields `unavailable (<which field>)`.
3. `--from/--to` in `build-census.mjs`: the only change to that file; messages outside the window are not counted; a test pins that a window covering the whole fixture equals the unwindowed run.
4. Fixtures: a trimmed lead session with two gaps (one under, one over 30 minutes), a record with all fields, a ledger day with one answered and one unanswered ASK, a tiny git repo built by the test with one post-acceptance fix commit touching a territory file and one touching nothing in range.
5. Run it on the three real builds (loop-gates, census-complete, codex-parity records under `docs/work/`) and on the hand-run baseline, with the real census files where they exist; commit the four `.four-read.md/.json` outputs under `docs/work/evidence/`. Where a real build has no `Lead-session:` (all three today), the reader takes `--lead-session <id>` on the command line and the evidence file says the id came from the command line and how it was established (the census file names the lead session file it read; use that).
6. docs/census.md: the four definitions verbatim, one paragraph on how to run the read at accept time.

## Territory R2: the record knows its sessions (scripts/work-record.mjs, its test, GOALS.md)
1. New record fields `Lead-session:`, `Spec-session:`, `Spec-from:` in `FIELD_LABELS`; `open` takes `--lead-session <id> --spec-session <id> --spec-from <iso>`; the Claude and Codex hooks already know the lead's session id (`leadIdHint`), so docs/census.md says (R1 owns that file; R2 hands it the sentence): open the record with the id your host reports (the hook's hint line in context, not an environment variable that may be unset). Lane four owns skills/team-build/SKILL.md this week and carries the same sentence there.
2. `checkAcceptance` gains `lead-session-missing`; `accept` refuses without it (no override: a record without its lead session cannot be read). `Spec-session:`/`Spec-from:` missing is a WARN, not a refusal, and the read prints tokens as `partial (no spec slice)`.
3. `accept` also accepts `--four-read <json>` and copies the four `value`/`unavailable` lines under `Four numbers:` in the record; without it, `accept` still passes but the record shows `Four numbers: not run`. The prediction rule from the bearings is a sentence in docs/census.md, not a check: the lead writes the next build's predicted four numbers in the RESULT to skills-fable.
4. GOALS.md: correct every status line the evidence contradicts, at minimum: lead turns for the loop-gates build is 32 by the script (not 7 by hand); "nothing lost or stalled" is false for the census build (7.25 hour host stall, knowledge note of Sep 25 and the bearings assessment); the census clause of DONE is "not computable until the four-number read runs". Quote the source for each correction in the commit message.
5. Tests: refusal without lead session; WARN without spec fields; `--four-read` copied; GOALS.md has no line claiming a number this build's evidence contradicts (a test greps the specific stale phrases and fails if they return). No README.md edit: four lanes run this week and the changelog is written once at integration; the RESULT carries this build's one-line changelog entry instead.

## Not in scope
No new hooks or events, no scheduler, no Notion writes, no install, no release commit, no merge, nothing under skills/decisions/, skills/bearings/ or skills/team-build/ (lane four owns it), no README.md edit. No edits to `scripts/build-census.mjs` beyond `--from/--to`. No trailers; the git identity is never set by an agent. Push on green (Ben's rule of Sep 24); merge waits for Ben's word.

## Acceptance
- Sealed suite green on the branch; `check-acceptance` and `accept` pass on this build's own record with `--census` (fresh census at accept time, after the last review, lead file plus subagents dir) and `--four-read` on this very build (dogfood: the record shows its own four numbers, and `Lead-session:` is the fresh session that led it).
- Opus reviewers per territory with `JUDGMENT:` lines; one seam review across R1 and R2 (the record fields the read consumes).
- The RESULT to skills-fable carries: the four numbers for this build and for the three earlier builds and the baseline (values or `unavailable` with reasons), the script's `leadTurns` for this build, the lines added to `build-census.mjs` plus `work-record.mjs` (must be at most +150), and the predicted four numbers for the next build.
