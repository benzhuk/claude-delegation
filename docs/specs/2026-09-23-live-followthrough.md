# Live follow-through

Goal: useful verified outcomes through Codex, Claude Code and mixed collaboration. Ben reiterated continuous maximum useful parallel progress after the source wave. A partial native trial is not a global stop condition. Source changes, independent review, merges and session-only trials remain authorized. Persistent installation, schedule activation and durable knowledge publication retain their existing scoped decisions.

Base: 13b9e01. Parent owns docs/work and integration metadata. Three independent initial lanes follow; reports under C:/Users/benzh/AppData/Local/Temp/astra-followthrough-0923. No global test suite, background daemon, configuration edits, credential copies, peer messages or subagents inside lanes. Small local probes may run in parallel. Read authentic evidence selectively and never publish raw provider logs or private context.

## N — Claude native permission and useful output

Read docs/work/evidence/native-claude-pilot-response.md and recovered assessment. Prior raw data is at C:/Users/benzh/AppData/Local/Temp/delegation-native-claude-state-20260923-181752-de7b0f1587f54fbf8cd23b01502f0467. Diagnose the actual reason Write remained promptable using local CLI contracts and narrowly relevant primary-source evidence. Do not repeat the whole earlier investigation or equate no-prompts denial with root cause. Deliver the smallest supported corrected invocation, distinguishing an SDK/host limitation from a harness defect. No model retry until parent reviews that invocation; useful stdout output is a valid non-code delivery path if appropriate, not a claim of native filesystem persistence. No permission bypass flags. Own only N temporary reports/scripts, not repository code.

## X — Codex native event and plugin evidence

Inspect current executable/help/local schemas and existing package hook code. Read relevant authentic events only for structural ancestry and hook fields, not private task content. Determine a supported discriminator for lead vs child and actual plugin discovery/cadence seams; unknown must not disable working lead delivery. Read openai-docs skill at its catalog path; local source first, official docs only as fallback. Produce scout-x.md with source-backed capabilities, smallest session-only useful trial, and one implementation candidate if there is a demonstrated gap. No changes to installed config, no model invocation yet. Own only X temporary reports/scripts.

## S — source scout for remaining automation

Survey existing bearings and decisions pickup plus continuation/transport recovery. Produce scout-cadence.md and scout-recovery.md (each <=40 lines) for the smallest useful remaining source improvements. Identify existing helpers, pinned interfaces and adversarial checks. Do not introduce a new controller/scheduler or duplicate decisions truth. Map prospective file territories before proposing implementation; no code edits yet. Distinguish source work available now from activation requiring existing decisions. Compare 0.17 implementation to requirements, not stale plan wording. Own only S temporary reports.

## Delivery

Each lane reports evidence with file:line or primary URL, actual probes and unknowns, verdict first. Reports must name a concrete next action and falsifiable prediction. Parent adjudicates implementation contracts, opens build ownership, and pipelines reviews as results arrive. Stop the overall effort only at the goal or actual external dependency of every remaining useful authorized lane, never merely because a wave or status response is finished.
