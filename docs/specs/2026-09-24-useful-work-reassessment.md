# Resume from the product outcome, not the closed source queue

Authority: Ben repeatedly authorized continued useful work at maximum useful parallelism, approved merges, and challenged the prior stop on September24. This resumes that existing scope. Source edits/tests/reviews, read-only diagnosis, isolated nonpersistent probes and current plain skill mirroring remain authorized. No broader production configuration, policy bypass, external messages, knowledge publication or unchecked owner choice is inferred.

Observed failure: the prior stop disposition listed remaining outcomes as external dependencies without exhausting reversible diagnosis and preparation within those outcomes. Codex command admission remained unknown rather than proved externally blocked. Accepted records are not the complete product goal. Do not repeat earlier research or re-run a failed trial solely to get a green score.

Independent bounded lanes:

- `policy-diagnosis`: extend the prior native-codex-policy-boundary evidence by identifying the exact installed CLI admission path using local installed contracts/source and read-only diagnostic surfaces. Determine what --ignore-user-config + read-only + noninteractive approval implies, and whether the original fixture suppressed required existing setup. No denied command retry through another shell, widened sandbox, rules bypass, credentials, configuration change or provider trial. Produce a root-cause explanation or exact missing diagnostic and executable authorized next step, not a generic "needs human" label.
- `unmet-outcomes`: inspect remaining outcomes and their already prepared artifacts/scripts. Find finite useful work possible before owner choices: robustness or missing functionality in Done handling, memory retrieval/durability preparation, measurements, ordinary project entry. Separate actual authority barriers from tasks that merely precede them. Rank concrete changes by impact on useful delivery, avoid duplicate engines and already fixed defects. No production mutation or broad new research.
- `stopping-mechanism`: independently examine the prior stop decision against current continue/bearings/core/hook contracts and actual host status where available. Explain instruction vs activation vs scope-selection limitations. Propose the smallest meaningful correction if supported, with a discriminating test or operational evidence. Do not default to extra prompts, more turns, a new scheduler, or fabricated work.

Parent owns all work records, synthesis and admissions. Reports retain observed/inferred/unknown distinctions and precise source paths. Researchers do not edit source. Each report ends with the next authorized action and actual dependencies; negative results are allowed but a missing measurement is not automatically an external blocker. Read existing evidence first, investigate only the unresolved delta. ETA3minutes is a progress checkpoint; cap8minutes per initial lane. Source changes are separately scoped after findings, with existing build/review/integration workflow.
