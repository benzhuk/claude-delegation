# decisions-current: useful attended release, current human contract

Goal: safe, truthful human decision handling shared by Codex and Claude. Base integrate/decisions-current 34fd10c, inherited code and reports. Parent skills-a now owns this branch's release preparation. No main merge, push, live writes or configuration changes authorized by this specification.

Read release-audit.md in this directory. Fix only the four blockers with the smallest coherent change. Do not build unattended pickup or a new publisher/database. Main baseline is78cb46e.

Territory E owns skills/decisions/**, docs/specs/2026-09-22-decisions-current.md, docs/specs/2026-09-20-decisions-reader.md, docs/pane-setup.md, docs/GOALS.md, docs/goals/card.md in a new isolated worktree from34fd10c. No other lane touches these files.

1. Done is a human submission signal. Parser accepts exact legacy Done and Done (last cleared: <text>) as page control, preserving checked status and label. Reader exit1 includes checked Done even if unanswered options remain. Handback permits unchecked zero-open page and checked partial submission as a valid page shape; it must require all submitted input accounted before link handback, but must not require answering every still-open question. Skill directs agent to account captured choices/comments, then clear checked Done with actual America/New_York timestamp; if current input changed, account/reconcile it before clearing. No claim of atomic compare-and-swap or ABA protection. Checked Done itself grants no authority. Retain existing Waiting on you now/Closed shape checking. Meaningful regressions for all supported cases, warnings, and comments under ticked options.

2. Remove the unsafe existing-page auto-publication path rather than adding another synchronization framework. Keep goals-mirror render and pure parsing helpers. goals-mirror publish must refuse before any network mutation with actionable message to render and use existing notion-writing fresh targeted edits. Do not upsert even on --current none: wrong-target and concurrent creation risks are unnecessary for this attended slice. Remove dead mutating implementation and update existing tests/callers/prose to reflect intentional deprecation. Document deterministic renderer -> existing notion-writing reader and targeted update of agent-owned sections -> verify readback, preserve all surrounding human content, reconcile uncertain writes. Initial creation can use existing notion-writing under normal authority. The renderer is useful and testable; a duplicate live publisher is not needed. No new wrapper script. Gate5 attended live verification remains pending and now uses the existing safe writer, not the refused command.

3. Preserve fail-open switch policy but distinguish disabled from verified clean. When enforcement disabled, retain allowed exit0 if needed but emit explicit HANDBACK disabled and never HANDBACK ok. Skill's actual link-handback rule requires HANDBACK ok plus summary, exit0 alone is insufficient. Add meaningful caller/output regression. Disabled is not a verification pass.

4. Restore docs/GOALS.md and docs/goals/card.md exactly to main78cb46e in this candidate, removing the stale unreviewed rewrite from this feature. A separate goal reconciliation should preserve four measures and follow current agent-agnostic/speed-quality requirements. Update fixture expectations only if needed from concrete behavior. Change Sonnet-only runner instruction to mid-tier native provider equivalent referencing existing model tiers.

Tests: inherited focused decision suites via sealed scripts/run-tests.mjs. Ask parent for one verification slot. Existing helpers/fixtures reused, no new proof schema or tests asserting only wording unless they guard an actual conflicting instruction. Reviewer independently challenges behavior. Regression before/after evidence where applicable. No installed-host support claimed from synthetic tests.

Scout outputs scout-E.md <=40lines, read-only current source before worktree. No contract stubs needed: existing parser/renderer APIs retained except deliberate disabled publication CLI. Builder report E-report.md, state E-state.md, gate log E-gate.log in this directory. First report line VERDICT: PASS/FAIL/PARTIAL/BLOCKED, exact SHA, commands/results, limitations, Cause / Discriminating check / Fix location / Simplification. Predicts: removing duplicate mutating publisher and aligning one shared submission contract prevents data loss and removes a conflicting control mechanism.
