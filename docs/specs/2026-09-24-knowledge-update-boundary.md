# Preserve knowledge at the existing update boundary

Authority: approved rollout and existing-store curated publication decision in2026-09-24-authorized-rollout.md. Changes below prepare the reviewed implementation; no production mutation or publication until independent review. Windows is the proposed designated writer. No new scheduler, synchronization engine or automatic triage.

Evidence: Sol's sealed chezmoi2.72.1 fixture shows `--no-tty --less-interactive --error-on-conflict update` preserves live-only, source+live, and newly-managed existing targets, while unchanged/new-absent cases succeed. Without less-interactive the last conflict is overwritten. Native autostash preserves bytes but can unstage existing changes. PowerShell5.1 rejects the existing BOM-less UTF8 rendered template's four executable em-dash strings; ASCII strings parse. VBS currently does not wait and hides failure.

Pinned territory: high-tier builder prepares CANDIDATE COPIES ONLY in a unique temporary packet for machine-local `~/.local/bin/chezmoi-update.cmd`, `chezmoi-update-hidden.vbs`, tracked chezmoi `run_onchange_after_mirror-shared-skills.ps1.tmpl`, and `dot_claude/skills/triage/SKILL.md`. Record exact original path/hash for each. No live/source checkout mutation, git staging, triage invocation or publication by builder. Parent owns rollout and repository records.

Design: replace force with the verified native conflict flags; preserve exact child exit in CMD and wait/propagate it through hidden VBS. Correct only causal executable-string encoding in the template. Use one shared atomic exclusive writer/updater lock at an existing store-adjacent path; no lock-service or broad state-scan engine. The writer holds it from before curated edits through exact authorized publication verification, updater through pull/apply. A contender defers before touching Git/live files; an interrupted holder leaves a lock, never automatically expired. Release only the caller's own successfully acquired lock. Include attributable owner/operation metadata if achievable without another framework; never expose secrets. Preserve existing arguments/log paths and hidden execution.

Triage instructions must name this designated writer boundary, the actual tested acquire/release commands, and existing owner's-only invocation rule. This is cooperative writer exclusion, not proof arbitrary editors obey locks. Native refusal is defense for uncoordinated pre-existing edits. Do not overclaim eliminating every external-editor race.

Safety: no recursive deletion, no dirty-source reset/autostash restore magic, no credential or identity changes. Existing unrelated AppleDouble/mode changes remain byte/mode identical. Original tracked inbox content is not cleanup scope. Curated publication is an explicit allowlisted topic/index/digest operation after preservation checks, never a synthetic unreviewed inbox push.

Acceptance: sealed original failure/control evidence retained; new candidate CMD invokes stub success/failure with exact exit and VBS propagates exit using actual Windows Script Host. Two concurrent contenders cannot both enter; held/stale lock prevents child invocation and survives; owner normal path releases. Fixture must redirect every home/source/live/log path and cannot launch real production update. Render template via existing chezmoi and parse in PS5.1/PS7. Do not repeat already-proved six native conflict cases if invocation flags are unchanged; reuse their evidence. Builder returns exact candidate hashes, meaningful focused tests, limitations and concrete reviewed rollout commands; independent high-tier review required before installation. ETA4min/cap10min.
